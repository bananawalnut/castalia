# lean-seed.pin — the pointer to the currently-published HEAD-matching Lean seed archive.
#
# This is the ONE committed record that binds "the Lean source at this commit" to "the seed
# release artifact that links a VERIFIED node against it in minutes" (instead of an hours-long
# cold `lake` bootstrap of mathlib). scripts/fetch-lean-seed.sh reads TAG here to know which
# GitHub release to pull the platform-native seed asset from. TAG is stable; the
# .github/workflows/lean-seed.yml publisher deliberately never rewrites or rebases a human branch.
# A maintainer records the reference provenance below after publishing a new closure.
#
# The provenance triple (LEAN_TOOLCHAIN / MATHLIB_REV / DREGG_CLOSURE_HASH) is what fetch compares
# against the local checkout: if they match, the published seed is HEAD-compatible for THIS
# source; if they DRIFTED, fetch warns loudly that the pinned seed is stale (its Dregg2 slice /
# mathlib closure predates your source — the closure link may then need a local `lake`).
# Recompute the live values any time with:  scripts/lean-seed-key.sh
#
# ⚑ `DREGG_TREE_HASH` BECAME `DREGG_CLOSURE_HASH` on 2026-08-07 and it is a FLAG DAY. The old field
# was `git rev-parse HEAD:metatheory/Dregg2` — all 2246 modules, of which the archive contains 339
# (the `Dregg2.FFI` import closure, the only target build.rs builds). Every asset published under
# the old key is unreachable by the new asset name: re-cut with `gh workflow run lean-seed.yml`.
# The value below is the pre-cutover snapshot and is DEAD until the first new-scheme cut lands.
#
# build host — see docs/LEAN-SEED-ARTIFACT.md, "Cutting a seed release on lassie"), fetch fails
# loud and the only verified-node path is a local ./scripts/bootstrap.sh (the hours-long bootstrap).
#
# The provenance below is a REFERENCE snapshot of the current HEAD (recompute live with
# scripts/lean-seed-key.sh). Fetch resolves the asset from the checkout's live content key; the
# snapshot makes drift visible and is maintained explicitly after a verified asset is published.

TAG=lean-seed
LEAN_TOOLCHAIN=leanprover/lean4:v4.30.0
MATHLIB_REV=1c2b90b13009c65b090d95a83c98e248deafb6f1
DREGG_CLOSURE_HASH=3ff6efcbd92d181373f1a2bd28e61bb6b2faad2f
GENERATED_UTC=2026-08-30T04:57:07Z
NOTE=Linux x86-64 seed published from protected Castalia bootstrap tag castalia-bootstrap-v0.1.0-rc.1 and workflow run 33283563597 as immutable content-keyed asset libdregg_lean-Linux-x86_64-v4.30.0-55c2d229de854123.a.zst. Assets under this tag are CONTENT-KEYED (see scripts/lean-seed-key.sh), so TAG is stable and this provenance is a reference snapshot for the drift warning only — fetch-lean-seed.sh always resolves the asset from YOUR checkout's key, not from these values.
