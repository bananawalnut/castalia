[package]
name = "dregg-wasm"
version = "0.1.0"
edition = "2024"
rust-version = "1.94.0"
authors = ["Dregg Contributors"]
license = "AGPL-3.0-or-later"
description = "WebAssembly playground for dregg token primitives"

[lib]
# `cdylib` for the browser playground bundle; `rlib` so the integration tests
# (`tests/*.rs`, e.g. the card-in-the-tab loop) can link the crate as a library
# and drive its `#[wasm_bindgen]` surface under `wasm-pack test`.
crate-type = ["cdylib", "rlib"]

[features]
# Forwarded only by the in-browser integration-test command. dregg-pq additionally requires a
# wasm32 debug build before it accepts this input, so release bundles remain fail-closed.
wasm-test-unaudited-pq = ["dregg-pq/wasm-test-unaudited-pq"]

[dependencies]
wasm-bindgen = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde-wasm-bindgen = "0.6"
getrandom = { version = "0.3", features = ["wasm_js"] }
getrandom02 = { package = "getrandom", version = "0.2", features = ["js"] }
getrandom04 = { package = "getrandom", version = "0.4", features = ["wasm_js"] }

# Local crates (only WASM-safe ones). `no-lean-link` is THE platform gate
# (.docs-history-noclaude/FEATURE-HYGIENE.md §Lean): wasm32 cannot link libdregg_lean.a, so these
# crates compile their Rust fallback paths here — a statement about this target's
# linker, never about which guarantees native dregg has (there Lean is unconditional).
dregg-macaroon = { path = "../macaroon" }
dregg-trace = { path = "../trace" }
dregg-commit = { path = "../commit" }
dregg-circuit = { path = "../circuit", default-features = false }
# The whole-history light-client-in-the-tab (`bindings_lightclient`) verifies the
# recursive IVC aggregate, so wasm pulls the recursion tower (already on this graph
# transitively via dregg-sdk/prover + dregg-lightclient).
dregg-circuit-prove = { path = "../circuit-prove" }
dregg-cell = { path = "../cell", default-features = false }
dregg-cell-crypto = { path = "../cell-crypto" }
# THE ONE SHARED POST-QUANTUM LEAF — `dregg-pq`'s consolidated ML-DSA-65 (FIPS 204)
# from-seed derivation + fail-closed verify. The light-client boundary uses
# `dregg_pq::ML_DSA_PK_LEN` to length-check the enrolled ML-DSA roster CSV, and the
# hybrid-vote test mints its PQ vote halves through `dregg_pq::MlDsaKey` — the SAME
# audited primitive the committee enrolls, replacing wasm's direct `fips204` use.
# Pure-Rust (fips204 + ml-kem + x25519-dalek, all already in this graph), wasm32-safe.
dregg-pq = { path = "../dregg-pq" }
dregg-turn = { path = "../turn", default-features = false }
# The γ.2 bilateral AGGREGATE demo (`prove_bilateral_aggregate`) is proof
# PRODUCTION, so it links the prover CRATE rather than riding a `prover` feature
# that this graph never declared (it arrived only through `dregg-sdk`'s forward —
# exactly the invisible-unification hazard the extraction retires). `dregg-turn`
# above stays `default-features = false`; `dregg-turn-prover` depends on turn the
# same way, so this adds no feature pressure to the wasm card.
dregg-turn-prover = { path = "../turn-prover" }
# coord / intent are FFI-FREE by construction (they route their verified gates through their
# `verified_gate` seams, which only a Lean-boundary crate — never depended on here — registers).
# No feature needed: wasm32 simply doesn't pull a Lean impl, so the native-Rust differential decides.
dregg-coord = { path = "../coord", default-features = false }
dregg-intent = { path = "../intent", default-features = false }
# THE REFLECTIVE SUBSTRATE — the cap-bounded, attested read of a cell's four substances
# (the SAME `ReflectedCell::raw_fields` + `AffordanceSurface` the native cockpit inspector
# reads). gpui-FREE; its only deps are `dregg-cell`/`dregg-turn`/`dregg-types` — already in
# this graph — so it is wasm32-safe. `bindings_card`'s `InspectorWorld` reads its own in-tab
# cell's faces through it and generates the inspector view-tree the web renderer paints.
deos-reflect = { path = "../deos-reflect", default-features = false }
# THE PIJUL-SHAPED DOCUMENT CORE — `dregg-doc`'s patch theory (DocGraph · merge as the
# categorical pushout · conflicts-as-first-class-objects · resolve patches) PLUS its
# `cell-heap` ride (`to_heap_map` / `substrate_commit` — the sorted-Poseidon2 umem-heap
# boundary that IS a document's commitment, `src/doc_heap.rs`). `cell-heap` pulls JUST
# `dregg-cell` (empty default features — already in this graph) + `blake3` (already here);
# it does NOT pull `dregg-turn` (that is `substrate`), so it is wasm32-safe. `bindings_doc`'s
# `DocCollabWorld` drives fork → diverge → stitch → a first-class conflict → resolve, publishing
# the merged document to the doc-cell's umem-heap as a real verified turn over the in-tab executor.
dregg-doc = { path = "../dregg-doc", default-features = false, features = ["cell-heap"] }
# THE GPUI-FREE WEB RENDERER — `deos-view`'s `web` feature (`ViewNode` → an HTML/DOM string,
# `src/web.rs`): only `serde`/`serde_json`/`anyhow`, gpui-FREE + deos-js-FREE (the heavy gpui +
# mozjs deps are `optional` and gated on the `native` feature, never activated here). So
# `DocCollabWorld` renders its OWN view-tree to HTML in-tab via `render_html` (the conflict view
# re-renders wholesale after a resolve — the tree SHAPE changes, unlike the slot-repaint cards),
# the SAME renderer the cockpit's web projection bakes.
deos-view = { path = "../deos-view", default-features = false, features = ["web"] }
dregg-sdk = { path = "../sdk", default-features = false, features = ["no-lean-link"] }
# sdk pulls bulletproofs (via cell crypto) — already covered by the
# clear_on_drop override below, but listing here for grep-ability.
#
# `prover` USED TO BE NAMED HERE and no longer exists to name. The reason it was named is
# exactly why it had to die: this line had to re-add a feature it had just switched off,
# because WITHOUT it the SDK compiled its `#[cfg(not(feature = "prover"))]` v1 effect-VM leg
# and dangled on the deleted `dregg_circuit::EffectVmAir` / `effect_vm_p3_full_air` (the C7
# v1-deletion cutover). A feature whose OFF position does not compile is not an option, it is
# a trap with a name. Those `not(prover)` legs are deleted and the rotated IR-v2 producer +
# verifier are unconditional, so this graph gets them by default and cannot get anything else.
dregg-types = { path = "../types" }
# DreggDL: the REAL parse → lower → userspace-verify pipeline, so the TS SDK's
# `deploy.check()` calls the SAME checker the `dregg-deploy` CLI runs (not a
# TS re-implementation). Both are pure logic over the dregg-turn types already
# linked here (no Lean link of their own — they ride dregg-turn's no-lean-link
# via feature unification in this standalone workspace), so they are wasm32-safe.
dregg-userspace-verify = { path = "../dregg-userspace-verify" }
dregg-deploy = { path = "../dregg-deploy" }
# CapTP — needed to decode `HandoffCertificate` (carried by
# `Authorization::CapTpDelivered`) for `create_captp_delivered_auth`. Pure
# types + bs58 + postcard; wasm32-safe (already a transitive dep via dregg-turn).
dregg-captp = { path = "../captp" }
# Federation (pure types + consensus orchestration). The `runtime` feature
# (tokio + crossbeam transport) is non-wasm32, hence default-features = false.
# FFI-free by construction (the `verified_gate` seam, unregistered here).
dregg-federation = { path = "../federation", default-features = false }
# DFA — canonical route-table commitment for the governed-namespace app flow
# (RouteTableBuilder + route_table_commitment). Pure blake3 + serde + postcard;
# the heavy `dregg-federation` dep is optional and stays off here, so wasm32-safe.
# We do NOT depend on starbridge-governed-namespace / starbridge-subscription
# directly because they pull `dregg-app-framework`, which depends on axum +
# tokio(full) + reqwest (non-wasm32). The cell-programs those crates author are
# re-materialized from canonical `dregg_cell` types in `runtime.rs::app_programs`
# (the program *value* is canonical; only the assembly is local).
dregg-dfa = { path = "../dfa", default-features = false }
# Observability (EventLog + Emitter for live <dregg-activity> feed). All
# heavy deps (circuit/federation) already gated in its own Cargo.toml;
# wasm consumes with default-features=false for wasm32 safety.
dregg-observability = { path = "../observability", default-features = false }

# THE STORY SURFACE — `spween-dregg`'s verifiable CYOA, now BROWSER-PLAYABLE. Its
# `Driver`/`WorldCell` ride `dregg-app-framework`'s `EmbeddedExecutor` via the
# framework's wasm-clean CORE — `spween-dregg` depends on `dregg-app-framework` with
# `default-features = false`, dropping the `server` feature (axum/tokio-full/reqwest/
# tower-http, non-wasm32). So `<dregg-story>` compiles to wasm32 the same way
# `DocCollabWorld` does, and `bindings_story` is in the shipped bundle (no target gate).
spween-dregg = { path = "../spween-dregg" }

# THE DESCENT SURFACE — `dreggnet-offerings`'s daily, provably-fair, permadeath procgen
# roguelite (`daily_descent::daily_scene` — the SAME generator the offering + the no-cheat
# leaderboard deploy, so the world played in the tab is byte-identical to the one a stranger
# re-executes). `bindings_descent`'s `DescentWorld` drives it on the SAME wasm-clean executor
# `<dregg-story>` uses: `dreggnet-offerings`'s substrate deps are `spween-dregg` +
# `dungeon-on-dregg` + `dregg-app-framework` (all `default-features = false`, dropping the
# non-wasm32 `server` feature), plus `procgen-dregg` (the beacon → daily-seed wire) below — the
# `tlsn`/`tokio` edge is a `dungeon-on-dregg/tlsn-live` optional dep on the SEPARATE
# `dregg-zkoracle-live` crate and is off (it was a feature forward into
# `dregg-zkoracle-prove` until the 2026-07-27 split, which is a much longer reach to keep
# off a wasm bundle). So it crosses to the tab exactly
# like `spween-dregg`, and `bindings_descent` is in the shipped bundle (no target gate).
dreggnet-offerings = { path = "../dreggnet-offerings" }
# THE IN-HOUSE SPRITE — `dreggnet-sprite`: the deterministic generative renderer
# (`content-addressed asset → trait vector → composed vector SVG`), exposed in the tab as
# `spriteSvg(kind, assetIdHex)` / `traitsJson(kind, assetIdHex)` (`bindings_sprite`). Pure
# Rust SVG-string (no floats/trig/unordered iteration) — same asset ⇒ byte-identical SVG.
# Its deps are all already in this graph and wasm32-safe: `dreggnet-asset` (the AssetId
# content address — its `dregg-schema`/`spween-dregg`/`dregg-cell`/`dregg-app-framework`
# substrate is a direct wasm dep via `spween-dregg`), `dregg-dice` (the verifiable draw
# stream — already pulled by `procgen-dregg`), and `blake3`. So it crosses to the tab and
# `bindings_sprite` is in the shipped bundle (no target gate). Consumed READ-ONLY.
dreggnet-sprite = { path = "../dreggnet-sprite" }
# The content-addressed asset identity — `AssetId` (a blake3 content address), the sprite
# seed `bindings_sprite` decodes a hex id into. Consumed READ-ONLY; already in this graph
# transitively via `dreggnet-sprite`, declared directly here for the `AssetId` construction.
dreggnet-asset = { path = "../dreggnet-asset" }
# THE BEACON → DAILY-SEED WIRE — `procgen-dregg`: `daily_seed`/`CommittedSeed` (fold a committed
# beacon output into the day's dungeon seed) and `beacon::DailyBeacon` (verify a fetched drand
# `quicknet` reveal by BLS pairing, in the tab, before it will open a day). Pure `dregg-dice`
# (blake3 + bls12_381, pure Rust) + blake3; wasm32-safe.
procgen-dregg = { path = "../procgen-dregg" }

# THE VERIFIED CARD GAME, PROVED IN THE TAB — `dregg-multiway-tug`: the hidden hand
# committed as a Poseidon2 4-ary Merkle root, its per-play membership STARK + whole-match
# recursion fold. `bindings_multiway_tug` consumes its fold READ-ONLY: `HandTree`/`card_leaf`
# (the private hand + blinded leaf), `fold::{membership_leaf_for_play, LeafBundle, fold_match}`
# (the committed Phase-3 fold). Its deps are all already in this graph and wasm32-safe:
# `dregg-schema` (app-framework/cell/spween-dregg), `game-turn-slice` (circuit/circuit-prove/
# cell/turn/lightclient), plus circuit/circuit-prove/lightclient/spween-dregg/dreggnet-*/
# deos-view/procgen — no new heavy or non-wasm edge. Consumed, never modified.
dregg-multiway-tug = { path = "../dregg-multiway-tug" }

# Token crate without biscuit (biscuit-auth has complex deps)
dregg-token = { path = "../token", default-features = false, features = ["macaroon", "rand-deps"] }

# THE LIGHT CLIENT IN THE TAB (N12). `dregg-lightclient::verify_history` folds a
# whole finalized history into ONE succinct recursive aggregate and verifies it
# re-witnessing nothing — the anti-pale-ghost tooth, carried to the browser. It
# pulls `dregg-circuit` (recursion — already unified ON in this graph via
# observability/bridge; the STEP-0 `[patch]` above supplies the fork's
# `NativeBatchStark` variant) + `dregg-blocklace` (wasm-safe: blake3 + ed25519 +
# postcard + types, no tokio). `prover` is in its default set, so a plain dep
# carries the light client.
dregg-lightclient = { path = "../lightclient" }

# TRANSCLUSION IN THE TAB — Xanadu made honest, carried to the browser. The REAL
# `dregg://` finalized read NAMED as Ted Nelson's transcluded quote
# (`starbridge_web_surface::transclusion::TranscludedField::include` + the
# `WebOfCells` resolve/render path + the SNAPSHOT/LIVE dial). `bindings_transclusion`
# exposes the `#[wasm_bindgen]` entry the playground's `transclusion.js` drives:
# transclude a span → amend the source → the quote updates live → attempt a forge →
# it REFUSES with `FetchError::ContentHashMismatch`.
#
# This is the resolve/render path ONLY — it pulls `dregg-firmament` (whose deps are
# JUST `dregg-cell`/`dregg-turn`/`dregg-types`, all already in this graph; the
# `process-pd` libc backing is off), NOT the circuit prover or the recursion path
# (those ride `dregg-lightclient` above, untouched). `default-features = false` drops
# the cockpit `stream`/`futures-core` edge, which the pure resolve path never needs.
# The single `dregg-turn` instance unifies `no-lean-link` (this graph's pin) across
# firmament's plain dep — the same already-proven wasm executor `web-surface.js` runs.
starbridge-web-surface = { path = "../starbridge-web-surface", default-features = false }

blake3 = "1"
bs58 = "0.5"
base64 = "0.22"
postcard = { version = "1", features = ["alloc"] }
js-sys = "0.3"
x25519-dalek = { version = "2", features = ["static_secrets"] }
zeroize = { version = "1", features = ["std", "derive"] }
ed25519-dalek = { version = "2", default-features = false, features = ["zeroize"] }

# Indirect dep via bulletproofs (pulled in by dregg-cell when turn/coord/intent
# enable their default crypto feature). clear_on_drop's default build invokes
# a C compiler (build.rs -> cc -> clang --target=wasm32) which has no working
# wasm32 toolchain on most systems. The `no_cc` feature switches it to a pure
# Rust implementation. Declaring it directly here unifies the feature across
# the workspace so wasm-pack succeeds.
clear_on_drop = { version = "0.2", features = ["no_cc"] }
# Indirect dep via `hints` (pulled in by dregg-federation -> hints). Lockstitch
# uses inline x86_64 / aarch64 AES intrinsics unless the `portable` feature is
# enabled. The wasm32 target has neither, so we force `portable` here.
lockstitch = { version = "0.29", features = ["portable"] }
# Indirect dep via `dregg-token`'s `biscuit` feature (enabled by `dregg-turn`
# and `dregg-sdk` for the on-critical-path `Authorization::Token` verification).
# biscuit-auth's wasm32 `Instant` shim uses `performance_now()` + `TryInto`,
# both of which are only declared behind its own `wasm` feature — without it the
# crate fails to compile for wasm32. We force the feature here so unification
# applies it to the transitively-pulled copy. wasm-only: native already builds.
[target.'cfg(target_arch = "wasm32")'.dependencies]
biscuit-auth = { version = "6.0.0", features = ["wasm"] }

[dependencies.web-sys]
version = "0.3"
features = ["Performance", "Window"]

# Drives the `#[wasm_bindgen]` surface as a REAL wasm module under node /
# headless-Chrome (`wasm-pack test --node` / `--headless --chrome`). The
# card-in-the-tab loop (`tests/card_fires_a_verified_turn.rs`) runs the EXACT
# bytes the browser runs — a button-click → a cap-gated verified turn → the
# bound slot re-paints.
#
# 2026-07-16: this line USED TO SIT DIRECTLY UNDER `[dependencies.web-sys]`, which in TOML made it a
# stray key INSIDE web-sys's dependency spec (`{version, features, wasm-bindgen-test}`) rather than a
# dependency of this crate. So `wasm-bindgen-test` was never actually depended on, every
# `use wasm_bindgen_test::*;` in `tests/` failed to resolve, and NONE of this crate's tests could
# COMPILE — the concrete mechanism behind "wasm's tests are published untested". It was invisible
# because `wasm` is workspace-`exclude`d AND no push/PR CI job builds it (only the
# `workflow_dispatch`-only `pages.yml` / tag-triggered `publish-sdk-ts.yml`, neither of which runs
# tests). A dev-dependency is what this always meant to be.
[dev-dependencies]
wasm-bindgen-test = "0.3"
# `tests/cross_target_preimage_widths.rs` pins `dregg_bridge::present::hash_index` — a BLAKE3
# preimage that absorbed a bare `usize` and so built a different sibling felt on wasm32 than on
# a 64-bit host. `dregg-bridge` is ALREADY in this graph (transitively, via dregg-intent), so
# this adds no compilation the bundle was not already doing; it is a dev-dependency because the
# shipped cdylib has no reason to name it.
dregg-bridge = { path = "../bridge" }

# standalone (excluded from the root workspace — see root Cargo.toml)
[workspace]

# Mirrors the root workspace's patch (patch sections don't cross workspace
# boundaries; federation needs ark-serialize's serde_with feature from the fork).
[patch.crates-io]
ark-serialize = { git = "https://github.com/emberian/algebra", branch = "serde-integration" }
ark-serialize-derive = { git = "https://github.com/emberian/algebra", branch = "serde-integration" }

# Mirrors the root workspace's Plonky3 patch: the exact 82cfad73 `p3-fri` source
# with the additive backend-injection seam (`TwoAdicFriFoldBackend` /
# `CpuTwoAdicFriFold`) that `dregg-circuit-prove` now compiles against. Without
# it this standalone graph resolves the unpatched upstream rev and
# circuit-prove does not build here even though the root workspace is green.
[patch."https://github.com/Plonky3/Plonky3"]
p3-fri = { path = "../vendor/plonky3-fri-82cfad73" }

# THE FORK SEAM. This standalone workspace pulls `dregg-circuit` with its
# DEFAULT features via `dregg-observability`/`dregg-bridge` (both depend on it
# without `default-features = false`), so feature-unification turns on
# `prover` on the single circuit instance in this graph — which compiles
# `circuit/src/plonky3_recursion_impl.rs` + `ivc_turn_chain.rs`, both of which
# reference `RecursionInput::NativeBatchStark`. That variant lives in the pushed
# fork's `update-plonky3-rev` branch. `dregg-circuit`'s `p3-*` deps are
# `{ workspace = true }` and resolve against the ROOT workspace (circuit is a
# root member), whose git pin is the pushed fork tip
# `fc3c6dfac26e2082653d2a617a1740446ce33f05` (the `update-plonky3-rev` tip: a
# constant's VALUE is now a PREPROCESSED column of `ConstAir`, so every
# preprocessed commitment — and every `RecursionVk` derived from one — moves.
# It descends from 0a4a554's GPU recursion prove path by two commits. The root
# workspace dependencies pin the complete immutable revision
# `fc3c6dfac26e2082653d2a617a1740446ce33f05`; this standalone workspace resolves
# those transitive packages from that Git source too. There is no sibling path
# override, so a clean clone cannot silently build a different verifier.
