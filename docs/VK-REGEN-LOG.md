# VK-REGEN LOG — append-only audit trail of descriptor regen events

Every authorized descriptor install / provenance stamp appends one row
(written by `scripts/emit_descriptors.py`; see docs/VK-REGEN-CONTROLS.md).
Rows are never edited or removed; git history is the tamper-evidence.

## The `epoch` column and the ledger — and why the emit script cannot be the gate

`PersistentStore::CANONICAL_STATE_SCHEMA_EPOCH` (`persist/src/lib.rs`) is a **Rust constant that
any commit can bump**, while this file was **only ever appended to by `scripts/emit_descriptors.py`**.
The two diverged on 2026-08-01: `6441705e8` moved the epoch 20 → 21 without running an emit, the
row above it still read "Schema epoch UNCHANGED at 20", and nothing in the tree could say so — a
check placed inside the emit path would have reproduced that blind spot exactly. So the check is
keyed on the **constant**: `scripts/check-schema-epoch-log.py`, a row of `scripts/local-gates.sh`,
plus `dregg_persist::schema_epoch_log_row` — an in-crate test that reds for anyone who edits the
constant and runs `cargo test -p dregg-persist`.

Two machine-readable structures serve it, and a malformed one is a **failure, never a pass**:

- **The `epoch` column** on every event row below. `epoch:N` = the epoch in force once that row's
  change landed; `epoch:unchanged` = this row carries no epoch information; `epoch:unknown` = it
  cannot be determined from the record. The **last `epoch:N` row must equal the constant.**
- **The ledger** below: one row per value the constant has ever held in committed history. Its last
  row must equal the constant too, and every value `git` can see must appear in it.

⚠ **BACK-FILLED 2026-08-01, so this column is younger than the rows that carry it.** Values were
derived mechanically — `git show <that row's repo HEAD>:persist/src/lib.rs` — except for the four
rows that declare their own bump, which carry the value *after* the change (their recorded repo HEAD
predates their own bump commit). The 23 rows dated before 2026-07-23 are `epoch:unknown` and that is
not a hedge: the constant **did not exist** at their repo HEAD (it was introduced at 11 by
`99ecf54b8`, 2026-07-22).

⚠ **TWO ROWS OF PROSE IN THIS FILE ARE WRONG ABOUT THE EPOCH, and the column does not repeat them.**
The 2026-08-01T10:43:56Z row says "Schema epoch 16 -> 17"; the constant went **16 → 19** in one
commit (`f77496b65`, the commit that row describes). **No commit in this repository's history has
ever held 17 or 18** — `HORIZONLOG.md`'s "FLAG DAY: `CANONICAL_STATE_SCHEMA_EPOCH` 18 → 19" is wrong
in the same way, and `HORIZONLOG.md` says why in a parenthesis 100 lines later: "the 18 slot was
allocated to this change and then two sibling epochs interleaved while it was in flight". Prose
records what a lane *intended*; the column and the ledger record what the constant *did*.

⚑ **SEVEN `epoch:` CELLS WERE CORRECTED 23 → 22 ON 2026-08-07, AND THIS IS THE ONE PLACE THAT SAYS SO.**
Rows are never edited; the `epoch` column is a **back-filled annotation** (see the paragraph above),
and this corrects the annotation, not the record — every other cell of those seven rows is untouched.

The seven are `2026-08-01T21:56:48Z`, `23:39:56Z`, `23:42:23Z`, `2026-08-02T00:42:52Z`, `01:38:38Z`,
`14:39:55Z`, `14:52:13Z`. Each read `epoch:23` while the constant read **22** at that row's own
recorded repo HEAD — mechanically checkable, one `git show <head>:persist/src/lib.rs` each — and the
descent from the first correct `epoch:22` row that followed (`2026-08-03T00:25:42Z`) is what
`dregg_persist::schema_epoch_log_row` and `scripts/check-schema-epoch-log.py` had been reporting as
`EPOCH-GOES-BACKWARDS`. **The gate was right and the rows were wrong; nothing was widened.**

They were not mis-typed. 23 was being authored in a working tree at the time — the ledger row below
says so — and seven lanes recorded the epoch they were about to be under. **The constant did not
actually reach 23 until `522b9c763` on 2026-08-03T15:55:38Z, 42 hours later and by another hand.**
That is the whole finding, and it is a real one rather than a bookkeeping slip: for those 42 hours
the tree carried the epoch-23 *state format* (the Poseidon2 hash-LOCK slot landed at
`2026-08-01T21:56:48Z`) while `CANONICAL_STATE_SCHEMA_EPOCH` still read 22, so a store written in
that window was stamped 22 and holds 23-shaped fields. The causal link between that row and epoch 23
is not lost by this correction — the ledger row for 23 names the event row explicitly. The column
records what the constant *did*; the ledger records why it moved.

## SCHEMA EPOCH LEDGER

Every value `CANONICAL_STATE_SCHEMA_EPOCH` has held, in commit order. Reconstruct with
`git log -p --follow -- persist/src/lib.rs | grep CANONICAL_STATE_SCHEMA_EPOCH`; the gate checks
this table against exactly that walk, so a bump with no row here is red whether or not an emit ran.

| epoch | set by | when (UTC) | what it re-genesised |
|---|---|---|---|
| 11 | `99ecf54b8` | 2026-07-22T12:37:28Z | the constant is INTRODUCED, with the exact fields-root v11 encoding. Numbering starts at 11 (the fields-root version), not at 1; no store was ever stamped 1–10. |
| 12 | `5ec8ce76d` | 2026-07-26T11:31:08Z | `Cell.token_id` split into a NAME SALT and a separate ASSET. |
| 13 | `2892d0999` | 2026-07-30T23:31:02Z | fields octet: lanes 2..7 carry a hash of an injective preimage, closing the O(1) fields alias at the anchor. |
| 14 | `f10e5950e` | 2026-07-31T13:26:33Z | fields nonet: the anchor inherits the injection; `field_limbs8` DELETED. |
| 15 | `b20a2c50a` | 2026-08-01T00:45:48Z | A2/A3 accumulators made injective at the anchor. |
| 16 | `57105f387` | 2026-08-01T05:54:18Z | 4-ary membership node8 cutover — authorized-set root slots move from one felt in the low 4 bytes to eight u32 LE limbs. Event row 2026-08-01T05:24:11Z. |
| 19 | `f77496b65` | 2026-08-01T12:05:02Z | sorted-set NON-membership node8 cutover. ⚠ **THE CONSTANT JUMPED 16 → 19 IN THIS ONE COMMIT.** Its event row (2026-08-01T10:43:56Z) says "16 -> 17" and `HORIZONLOG.md` says "18 → 19"; **17 and 18 were slots two sibling lanes allocated in flight and neither was ever committed.** Reconstructible in one line, which is why it is stated rather than guessed. |
| 20 | `a62c48c7b` | 2026-08-01T12:21:49Z | cells_root widened (the cell key was ONE FELT and two ordinary creations halted consensus). ⚠ **NO EVENT ROW OF ITS OWN** — it landed between the 12:08:14Z and 17:10:00Z rows and is documented only in `HORIZONLOG.md`. Recorded here so the epoch history reconstructs from this file alone. |
| 21 | `6441705e8` | 2026-08-01T17:20:41Z | key nonet: `canonical_32_to_felts_8` repaired (`& 0x3F` had discarded bits 6–7 of every fourth byte, packing an Ed25519 key and its negation to one octet). **This is the bump that motivated the gate** — see the retroactive event row at 2026-08-01T13:20:41Z. |
| 22 | `6342defa2` | 2026-08-01T20:46:50Z | receipt-log root `iroot`: 1-felt fold → 8-lane `rchain8`, and the empty-log root moved from `BabyBear::ZERO` to `A2[RCE2, 0]`, so every anchor on both paths moves. Event row 2026-08-01T20:40:00Z. |
| 23 | `522b9c763` | 2026-08-03T15:55:38Z | **TWO causes, one epoch.** (a) the Poseidon2 hash-LOCK slot: `cell::program::eval::hash_preimage32`'s `HashKind::Poseidon2` arm went from `felt_to_bytes32(hash_bytes(p))` — ONE felt in bytes 0..4, 28 bytes zero — to `digest8_to_bytes32(hash_bytes_8(p))`. Every persisted `CellState.fields[i]` holding a Poseidon2 `PreimageGate` commitment or `KeyRotationGate` next-keys digest re-genesises. Event row 2026-08-01T21:56:48Z. (b) **THE OWNER KEY'S NINTH LANE (2026-08-02).** Both rotated producers now write all nine base-`2^29` nonet lanes of `cell.public_key` — lane 8 to `B_PUBKEY_NINTH_LANE` (in-block limb 186), an ABSORBED pre-limb — so **every** cell's `state_commit` moves, not only key-rotation ones. Event row 2026-08-02T14:53Z. ⚠ **WHY (b) DID NOT GET ITS OWN EPOCH 24, stated rather than glossed:** at authoring time 23 was uncommitted and **nothing anywhere was stamped 23**, so there was no store to migrate between 23 and 24 — a second number would have been migration theater. ⓘ **CORRECTED 2026-08-07:** this row said "23 has never been committed (`HEAD:persist/src/lib.rs` still reads 22)" and carried `(this working tree)` in its `set by` cell. Both were true when written and both went stale on 2026-08-03T15:55:38Z, when `522b9c763` ("sweep-up (sorry claudes)") committed the bump — 42 hours after the change this row describes landed, and by another hand. That 42-hour gap is the whole reason the seven event rows above had to be corrected 23 → 22 (see the header), and it is why a `(this working tree)` marker is a claim with a shelf life: nothing re-reads it when the tree it names becomes history. A separate 24 DID eventually arrive, from `5f216e915` and for an unrelated cause — see its row below. |
| 24 | `5f216e915` | 2026-08-07T08:28:45Z | **THE FINALIZATION-VOTE PREIMAGE ABSORBS A PER-TURN VALUE (v3 → v4).** The signed message went from `"dregg-finalization-vote-v3" ++ block_id ++ merkle_root` to the same prefix at `-v4` followed by a framed `receipt_stream_root` (`0x00` absent / `0x01` ++ 32 bytes, so an absent stream can never alias a present one) — no `>= threshold` committee signature in this tree had ever covered a value derived from the TURN. **Re-genesises:** every persisted `StoredAttestedRoot::finalization_quorum` signed under v3; `verify_finalization_quorum` REFUSES it rather than reinterpreting, and `enforce_canonical_state_schema_epoch` refuses a store stamped 23 outright. The gossiped `FinalizationVote` wire gained a field and postcard is non-self-describing, so a mixed-version mesh fails to DECODE, not merely to agree — every node upgrades together. **What re-emits:** no VK, no descriptor, no PI count, no chip arity; the LEAN SEED does — a seed exporting the v3 `dregg_finalization_quorum` returns `"ERR"` on every v4 wire and `verified_quorum_reached` fails CLOSED until it is republished. ⚠ **THIS ROW WAS MISSING UNTIL 2026-08-07** and `scripts/check-schema-epoch-log.py` reported it as `LEDGER-INCOMPLETE`: the bump ran no emit, and only `scripts/emit_descriptors.py` appends here — the exact blind spot this gate was placed OUTSIDE the emit path to catch. Reconstructed from the commit's own "WHAT BROKE / WHAT MUST BE RE-GENESISED — schema epoch 23 -> 24" section, not guessed. No event row of its own. |
| 25 | `8c90ba1a0` | 2026-08-07T10:41:34Z | **THE SHIELDED SPEND PINS THE COMMITTED ACCUMULATOR (seam #15).** `ShieldedTransferPayload.merkle_root` DELETED — it was the PROVER's choice of commitment-tree root and was compared against nothing, so an attacker built their own tree holding a note that was never committed and honestly proved membership at that tree's root. `apply_shielded_transfer` GATE 0 now reads `note_shielded.root8()` from live executor state and every input's complete-spend proof is judged with it as `piCommitted`. Re-genesises every store: a stamped-24 store is REFUSED, not migrated; the devnet is re-genesised. Event row 2026-08-07T06:50:00Z. |
| 26 | `5f33b6b43` | 2026-08-07T00:00:00Z | **THE SHIELDED VALUE LINK IS IN THE AIR; THE PEDERSEN LEG IS DELETED.** `ShieldedTransferPayload` loses `input_legs`, `output_legs`, `output_range_proofs`, `conservation`; `ShieldedInputPayload` loses `legacy_value_binding`, `wide_value_binding`, `wide_value_proof`; `ShieldedLeg` is DELETED and replaced by `ShieldedOutputPayload { note_commitment, link_proof }`. Epoch 25's lane named the residual verbatim — the Pedersen leg's own `v` was tied to the STARK-side `v` by a TRANSCRIPT and by no circuit equality (`verify_value_link` ran in tests only) — so a note worth 1 funded legs worth 1_000_000 and `verify_full_conservation_bytes` cleared over the legs. Not checkable across the two systems; the value is now read off ONE set of canonical 16-bit limb columns by the Lean-emitted `dregg-shielded-transfer-value-link::v1`. RE-GENESIS: every shielded-transfer `effects_hash` moves again; a pre-cutover payload REFUSES to decode; a store stamped 25 is refused, not migrated. |


## EVENT ROWS

| when (UTC) | operator | mode | HEAD:metatheory/Dregg2 | repo HEAD | source dirty | changed | epoch |
|---|---|---|---|---|---|---|---|
| 2026-07-08T01:11:04Z | ember@nextop.local | stamp-existing | 0ec5a7d198833da3095707cdd2ca7408280428de | e2695c236a20b2b8e30615c516965f2f4ffe74b9 | YES | (stamp only) | epoch:unknown |
| 2026-07-08T01:11:21Z | ember@nextop.local | stamp-existing | 0ec5a7d198833da3095707cdd2ca7408280428de | e2695c236a20b2b8e30615c516965f2f4ffe74b9 | YES | (stamp only) | epoch:unknown |
| 2026-07-12T01:27:23Z | ember@nextop.local | emit | 55e8e44db5a1754b1edc3e4d11f575b69934fadc | 73c3cb380916dfbd2d1f9591ca7b19e178bfe25e | YES | dregg-cross-cell-conservation-v2.json, rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, rotation-wide-transfer-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs | epoch:unknown |
| 2026-07-12T05:28:47Z | ember@nextop.local | emit | 09d05bf6e8130961304192c09a432900d1100d8a | 18846d7e1846111196fa225937dd7cc616dd01c3 | YES | rotation-wide-registry-staged.tsv, rotation-wide-transfer-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs | epoch:unknown |
| 2026-07-12T05:45:44Z | ember@nextop.local | stamp-existing | b70336374b0b4356378769985d6b7ded19b3e797 | 62de9e359db49df163b5770d5c9536c02de29a33 | YES | (stamp only) | epoch:unknown |
| 2026-07-12T05:55:10Z | ember@nextop.local | emit | f83dedecd00094790acc2c305fa5e6a93d21d531 | 8e65c07829a27eca0606d8f6794ba0347cb50368 | YES | rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs | epoch:unknown |
| 2026-07-12T13:22:54Z | ember@nextop.local | emit | a416e68ed588be4f71a0c473c721e5d277aec118 | fced697a4fa61a0a799f42800217a20ab19119f4 | YES | rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs | epoch:unknown |
| 2026-07-13T14:59:13Z | ember@nextop.local | emit | a03fdd7df3d91cf68c0d76a818211d20881be211 | 4dd7adee0b08a5e48f77064d5902b18f91310852 | no | rotation-wide-registry-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs | epoch:unknown |
| 2026-07-13T16:05:12Z | ember@nextop.local | emit | 296e504c0b6e80ded0e2f4bf5c643d0b530b9fcc | f9d9efd0df9a59ddb40eca5284ef0aa23993390d | YES | rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs | epoch:unknown |
| 2026-07-13T21:10:47Z | ember@nextop.local | emit | d705df25b69f1a7d9501991dc89c558e6ed34557 | 499467f7076e92ec9fe7161351493ec9c0cd54d9 | no | rotation-v3-staged-registry.tsv, circuit/src/effect_vm_descriptors.rs | epoch:unknown |
| 2026-07-13T21:43:12Z | ember@nextop.local | emit | 12b728d9b8ca99ef8cbc150d5d36c21c6abc2ad3 | 722cb4ebcd5456d03a2779e1b03bf5583cecea2f | no | rotation-v3-staged-registry.tsv, circuit/src/effect_vm_descriptors.rs | epoch:unknown |
| 2026-07-14T05:01:11Z | ember@nextop.local | emit | e337666840868ff974c59adc103a827597b69ac9 | 23c0a30e87966749e3c480dfde11537ccbee146d | YES | rotation-v3-staged-registry.tsv, circuit/src/effect_vm_descriptors.rs, circuit/src/effect_vm/layout_generated.rs | epoch:unknown |
| 2026-07-16T14:56:47Z | ember@nextop.local | emit | cca5ff5e8c7f47dbd4c90aa3d5b3e76326450405 | 840db3fe6dca838afc0a8fb9b47f07eefbab680e | YES | by-name/predicate-arith.json, circuit/src/effect_vm/layout_generated.rs | epoch:unknown |
| 2026-07-16T17:24:44Z | ember@nextop.local | emit | 09e9e01e122b9fac57206dd803c81ec7adf373f6 | 2ec5f4e094f7a3f5f1781a7ff566a50725d149ca | YES | by-name/predicate-arith-gt.json, by-name/predicate-arith-inrange.json, by-name/predicate-arith-le.json, by-name/predicate-arith-lt.json, by-name/predicate-arith-neq.json, circuit/src/effect_vm/layout_generated.rs | epoch:unknown |
| 2026-07-16T19:31:28Z | ember@nextop.local | emit | 2f4ace305ff37bdd0326bb1275761f640b85fd53 | ce164d75947628260bdccdd090a446a4f92460c7 | YES | by-name/predicate-arith-gt.json, by-name/predicate-arith-inrange.json, by-name/predicate-arith-le.json, by-name/predicate-arith-lt.json, by-name/predicate-arith-neq.json, by-name/predicate-arith.json, … +1 | epoch:unknown |
| 2026-07-16T20:55:29Z | ember@nextop.local | emit | f63d5886af930f2e709eae1949f41fc3134da858 | 2a01b0f31c56ba1b77ff974dbfd611b389e5c4ed | YES | by-name/attested-fact-membership.json | epoch:unknown |
| 2026-07-18T07:36:59Z | ember@nextop.local | emit | 45c734198995c5da69a33b278a10b878c41cc2c2 | 74aaed5e0b8c52bca350e84ce10f2459d8168629 | YES | by-name/automatafl-step.json | epoch:unknown |
| 2026-07-18T09:08:01Z | ember@nextop.local | emit | c76bd0761bd1eef3016088fce8427451441db148 | f52207f3314a48151862405e0059775837bd6dd5 | YES | by-name/automatafl-resolve.json | epoch:unknown |
| 2026-07-18T09:42:37Z | ember@nextop.local | emit | e1c97ee0955da92301ef4494af4dea347b8d94cb | c1cabd139867d67014da8c0d4808931b98e21714 | YES | by-name/automatafl-resolve.json | epoch:unknown |
| 2026-07-18T10:36:54Z | ember@nextop.local | emit | c52f7821b037ba0ed5ce666ce64ab1244ef355d2 | d4b8e7e7b42a1e32bc9b7b07b8e385f274573b69 | YES | by-name/automatafl-resolve.json | epoch:unknown |
| 2026-07-18T21:51:29Z | ember@nextop.local | emit | 7aac2659f4ccd7f04a7921bc5dcf725d4f05c7a0 | 649788aae2b2a875e3d3dd1026185246b6c79dc7 | YES | rotation-wide-registry-staged.tsv, rotation-wide-transfer-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs | epoch:unknown |
| 2026-07-19T01:12:18Z | ember@nextop.local | emit | bfd4f441749d844dbb44afd6348267be6102fc15 | 0566f8fba59d74540b678974049edc249558d839 | YES | by-name/automatafl-step.json, rotation-wide-registry-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs | epoch:unknown |
| 2026-07-19T08:40:22Z | ember@nextop.local | emit | bfd4f441749d844dbb44afd6348267be6102fc15 | 0566f8fba59d74540b678974049edc249558d839 | YES | targeted full-VK8 custom row: rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs | epoch:unknown |
| 2026-07-23T06:17:46Z | ember@nextop.local | emit | 3b930708e70c5d1a7736ee57c64160f9d63f96c5 | 2e7f6e65c3a901eeb93fe0e00c890f9803357ff7 | YES | by-name/automatafl-step-n11.json, by-name/automatafl-step.json, by-name/dark-amm-private-v1.json, by-name/private-book-bfv-threshold-terminal-q0-b80.json, rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, … +2 | epoch:11 |
| 2026-07-24T19:10:59Z | ember@nextop.local | emit-targeted (E7 by-name narrowing) | b5f736147a2918e38b6f6329c5d79f101c85e3fc | 0a445a1775b1bc3fec6fb11393d1223d0f599dd4 | YES | by-name/poseidon2-hash-arity2.json, by-name/merkle-membership-depth2.json (bytes from the machine-checked `#guard` pins; the canonical emit reproduces them byte-exact — verified no-op in `check-descriptor-drift`) | epoch:11 |
| 2026-07-24T21:18:19Z | ember@nextop.local | emit-targeted (E7 by-name narrowing, batch 2) | 6b44e7481d0be375313661c3a39661f6eaa81aab | 3f47a93023301cd436870ef88290697ca4ef4f81 | YES | by-name/{dfa-routing, turn-chain-binding, bound-presentation, blinded-membership, merkle-membership-4ary-general, dregg-shielded-spend-pinned-root-v1, dregg-shielded-value-link-conserve-ranged-v1, dregg-shielded-wide-value-link-conserve-v1, adjacency-membership, dyck-parse}.json — 10 members, -133 committed columns (bytes from the machine-checked `#guard` pins; the canonical `emit_descriptors.py` reproduces all ten byte-exact, its only would-change entry being another lane's routing ghost) | epoch:11 |
| 2026-07-25T00:26:45Z | ember@nextop.local | emit | 924e8b8d95b43475a2ea674e32a127a0ec569fb2 | 656120b19e98f5a2a13117f63faf02941c69037e | YES | by-name/guarded-hiding-span-m0-wide-blinded-commit-blind5-v1.json, dregg-effectvm-capreshape-v1.json, circuit/src/cap_reshape_descriptor.rs | epoch:11 |
| 2026-07-26T06:20:45Z | ember@nextop.local | emit | 29fd778cb95220ccdc3dfde8f5d1b9464b7ca6fa | e132a08e172afde945208ca469ac120b2ee6d8c9 | YES | PROVENANCE.json: by_name_sha256: `dfa-routing-table-exact-public-v1.json` was emitted but has NO row in the stamp, PROVENANCE.json: by_name_sha256: `dregg-eth-lightclient-verify-v1.json` was emitted but has NO row in the stamp, PROVENANCE.json: by_name_sha256: `dregg-midnight-lightclient-verify-v1.json` was emitted but has NO row in the stamp, PROVENANCE.json: by_name_sha256: `dregg-solana-lightclient-verify-v1.json` was emitted but has NO row in the stamp, PROVENANCE.json: by_name_sha256: `dregg-tm-lightclient-verify-v1.json` was emitted but has NO row in the stamp | epoch:11 |
| 2026-07-26T07:20:09Z | ember@nextop.local | emit | a4fdf397cc7214ac48b86e19da38121279d8710c | 76eb33d696d270269c5d44ae957ca30fc63a5437 | YES | rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs, circuit/src/effect_vm/e1_compact_generated.rs, circuit/src/effect_vm/s2_compact_generated.rs, … +3 | epoch:11 |
| 2026-07-28T14:17:35Z | ember@nextop.local | emit | a24dff2a483975d53bcb5d2d4cd790df3f2b44b6 | 2cfe16bd98d2bc77608e199b16f9f026d5bd3f91 | YES | rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs, circuit/src/effect_vm/e1_compact_generated.rs, PROVENANCE.json: descriptor_sha256: `rotation-v3-staged-registry.tsv` pin does not equal the emitted bytes, … +2 | epoch:12 |
| 2026-07-29T10:43:51Z | ember@nextop.local | stamp-existing | b654e03150370c19b427a6af043a4428f3c2c51e | ce4ce33f08cb8ff6a19379be74e8c63021010ef2 | no | (stamp only) | epoch:12 |
| 2026-07-29T10:45:25Z | ember@nextop.local | stamp-existing | b654e03150370c19b427a6af043a4428f3c2c51e | ce4ce33f08cb8ff6a19379be74e8c63021010ef2 | no | (stamp only) | epoch:12 |
| 2026-07-29T10:46:23Z | ember@nextop.local | stamp-existing | b654e03150370c19b427a6af043a4428f3c2c51e | ce4ce33f08cb8ff6a19379be74e8c63021010ef2 | no | (stamp only) | epoch:12 |
| 2026-07-30T15:45:19Z | ember@nextop.local | emit | 8d65c83779edbc0b3dfb402fe5a5af4164d45f5f | 90c6ee586965e35a765e22daf5a43896f13a3725 | no | PROVENANCE.json: by_name_sha256: `mina-fixture.json` was emitted but has NO row in the stamp, PROVENANCE.json: by_name_sha256: `pasta-rcb-windowed-trace-1024.txt` is pinned by the stamp but NO emitter produces it, PROVENANCE.json: by_name_sha256: `pasta-rcb-windowed-trace-64.txt` is pinned by the stamp but NO emitter produces it | epoch:12 |
| 2026-07-30T18:00:54Z | ember@nextop.local | emit | c65da7c7c5efd10c29c54d1051e38aec315325d9 | b1d3b138151e0f95eb89b5841840b584b510b0bb | YES | rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs, PROVENANCE.json: descriptor_sha256: `rotation-v3-setfield-value8-staged-registry.tsv` is pinned by the stamp but NO emitter produces it, PROVENANCE.json: descriptor_sha256: `rotation-v3-staged-registry.tsv` pin does not equal the emitted bytes, … +2 | epoch:12 |
| 2026-07-30T23:54:01Z | ember@nextop.local | emit | 7ef53baf2ea31bb06a0f96682399ce6b9cbd9f6d | a33aaeb133d4fb660d2ae3ece2d6d804628be554 | no | rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs, PROVENANCE.json: descriptor_sha256: `rotation-v3-staged-registry.tsv` pin does not equal the emitted bytes, PROVENANCE.json: descriptor_sha256: `rotation-wide-registry-staged.tsv` pin does not equal the emitted bytes, … +1 | epoch:13 |
| 2026-07-31T07:03:54Z | ember@nextop.local | emit | 298d50ecb49c5c42538e96a13a9453d7962f7b62 | 24599803e1128f5012584787016e527001c6ec1f | no | by-name/dregg-shielded-spend-pinned-root-v1.json, by-name/faithful-note-spend-exact-v3.json, rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, rotation-wide-transfer-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, … +10 | epoch:13 |
| 2026-07-31T14:33:48Z | ember@nextop.local | emit | 0b70b5d2f7d209765ab779b1dac2c70d427a9d8f | 96001f9098ebfd784fe97dcadaba4b5500665f2a | no | rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, rotation-wide-transfer-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs, circuit/src/effect_vm/e1_compact_generated.rs, … +6 | epoch:14 |
| 2026-07-31T16:15:49Z | ember@nextop.local | emit | 24fd6197ad323e3ece875e930ab6ff1ddcf04bd3 | 1d05b1819985b0a293c237420fddb18d8e7ec3ac | no | rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, rotation-wide-transfer-staged.tsv, rotation-wide-umem-welded-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs, circuit/src/effect_vm/e1_compact_generated.rs, … +6 | epoch:14 |
| 2026-08-01T05:24:11Z | ember@nextop.local | emit-targeted (membership node8 cutover) | d65cf554acdc35ccc324f0cee784ae00c759b9d6 | 392b02dd65ac995f36a05c92c41f1a87c6d65fb4 | YES | by-name/merkle-membership-4ary-wide-general.json (NEW, width 90 / 16 PIs), by-name/blinded-membership-4ary-wide.json (NEW, width 99 / 16 PIs), by-name/merkle-membership-4ary-general.json (DELETED, width 11 / 2 PIs), by-name/blinded-membership-4ary-depth2.json (DELETED), by-name/blinded-membership-4ary-depth8.json (DELETED), metatheory/EmitByName.lean (routing: 2 narrow rows -> 1 wide row, 77 -> 76) — felt-width finding #9 node8 CUTOVER: the 4-ary membership node went from ONE BabyBear felt (2^15.45 collision, forged in `circuit/tests/membership_forge_tooth.rs`) to the full 8-felt `A16(A16(c0\|\|c1)\|\|A16(c2\|\|c3))` fold (2^123.63). VK ROTATES for both families; the wire dispatch prefixes changed so pre-cutover proof identities answer UnknownAir and are REFUSED, not reinterpreted. Schema epoch 15 -> 16 (authorized-set root slots re-genesis: the 32-byte encoding moved from one felt in the low 4 bytes to eight u32 LE limbs). Bytes taken from the machine-checked Lean `#guard` byte-pins (`MEMBERSHIP_4ARY_WIDE_GOLDEN`, `BLINDED_MEMBERSHIP_WIDE_GOLDEN`) | epoch:16 |
| 2026-08-01T10:43:56Z | ember@nextop.local | emit-targeted (adjacency / non-membership node8 cutover) | 84022de846edb7d95e58890d57614034c560b272 | 2e14776e711276cdaef7b6bada6c8d2bbee174c0 | YES | by-name/adjacency-membership-wide.json (NEW, width 88 / 26 PIs / 132 constraints), by-name/adjacency-membership.json (DELETED, width 18 / 5 PIs / 34 constraints), metatheory/EmitByName.lean (routing: the narrow row REPLACED by the wide row, length stays 76), metatheory/Dregg2.lean (import) — felt-width node8 CUTOVER on the sorted-set NON-MEMBERSHIP family. Every node of the retired `dregg-membership-adjacency::poseidon2-v1` was ONE BabyBear felt: the per-level lookup was `chipLookupTupleNarrow [left,right] par` (arity-2 NARROW bus, `out0` alone), the chain was ONE window, and both leaf PIs plus the root PI were single felts. Codomain 30.907 bits => COLLISION **2^15.45** (the binding figure: the attacker chooses both sides), second-preimage 2^30.91. Now `adjacency_node8(l,r) = A16(l \|\| r)` — one arity-16 `node8` absorb, all 8 output lanes bound, an 8-lane chain, 8-felt leaves and root: COLLISION **2^123.63**, second-preimage 2^247.26. THE TOOTH IS EXHIBITED: `circuit/tests/adjacency_forge_tooth.rs` (5/5) finds a REAL collision in 20,879 evaluations (~2^14.3), builds a sorted 16-leaf tree in which a key at leaf index 2 — a GENUINE MEMBER — is certified ABSENT by the retired acceptance predicate (the forged bracket is honestly consecutive and honestly reaches the committed root), then shows node8 separating the pair and the deployed prover/verifier refusing. VK ROTATES; the descriptor NAME changed to `dregg-membership-adjacency-wide::node8-v1`, so a pre-cutover proof identity resolves to `None` and is REFUSED, not reinterpreted. Schema epoch 16 -> 17 (every non-membership set-commitment slot re-genesises: 32-byte encoding moved from one felt in the low 4 bytes to eight u32 LE limbs; the strict bracket moved from a lane-0 u32 compare to 8-felt lexicographic). DELETED, not deprecated: `circuit/src/adjacency_witness.rs` and the retired hand-mirror in `adjacency_membership_emit_gate.rs`. Bytes taken from the machine-checked Lean `#guard` byte-pin (`AdjacencyMembershipWideEmit.ADJACENCY_WIDE_GOLDEN`) | epoch:19 |
| 2026-08-01T12:08:14Z | ember@nextop.local | emit-targeted (chip-arity repair) | 8fb9f57034b458ef1467f5cb2375fb980d61a7c1 | 425ec7d87f3019d331ce27ac13153690c19a93ba | YES | by-name/guarded-hiding-span-m0-wide-blinded-commit-blind5-v1.json + its `PROVENANCE.json` `by_name_sha256` row. ⚑ **THE FLAG DAY**: the SOLE emitted hidden-span descriptor asked the Poseidon2 chip for arity 8 (span digest) and arity 14 (wide-blind commit tooth); the deployed `Ir2Air::Chip` admits only the roots of its degree-7 admission product `{0,2,3,4,7,11,16}` (`descriptor_ir2.rs:3073`), so BOTH constraints were unsatisfiable and the descriptor had never proved and could not have. Padded to the next admitted arities 11 and 16 in `GuardedHidingSpanWideBlindEmit.lean`. The tuples were already zero-padded to `CHIP_RATE` by `padToE`, so the emitted bytes change in EXACTLY TWO PLACES — the two arity tags — and every column, PI slot, trace width (53) and PI count (24) is unchanged. **What re-emits:** this one descriptor. **What changes meaning:** `span_digest8` and `hole_commit` are different values for the same opening (different chip domains), so any stored hidden-span opening must be recomputed; there are none. **What now refuses to load:** nothing — the old bytes still parse, and they still cannot be proved, which is now demonstrated by `circuit/tests/guarded_hiding_span_prove.rs::the_pre_repair_arities_do_not_prove`. Bytes are the machine-checked `emitVmJson2` `#guard` output, taken from Lean directly rather than through the full `emit_descriptors.py` sweep because three sibling lanes hold WIP in `metatheory/`; verified byte-identical to the Lean emission. | epoch:19 |
| 2026-08-01T17:10:00Z | ember@nextop.local | emit-targeted (MapAbsent law-#1 cutover: the live double-spend gate's AIR moves from hand-written Rust to a Lean emission) | b03704fbe416b07dae312064be3274669b5e89bc | d275360b3edeee8f2519b9fc70dbc84eeefb4ba5 | YES | **NO `circuit/descriptors/*.json` CHANGED — no descriptor re-emits.** What changed is the AIR IMPLEMENTATION: `Ir2Air::MapAbsent`, ~120 lines of hand-written `assert_zero`/`lookup_key` for the in-circuit non-membership gate a note spend rides through `noteSpendVmDescriptor2R24`'s `nullifierFreshOp`, is **DELETED** and replaced by `Ir2Air::LeanTable` walking a new artifact, `circuit/descriptors/table-airs/dregg-ir2-map-absent-v1.json` (NEW, 79,347 bytes, sha256 `3884c5ab8dc3664338065eb49546d9ad1d694f0bfa73019440f111504f3c9ca4`), emitted by `Dregg2/Circuit/Emit/MapAbsentTableEmit.lean` over the new `Dregg2/Circuit/TableAirIR.lean` IR. **What re-emits:** nothing — the descriptor set is untouched. **What ROTATES:** `faithful_note_spend_exact_v3_identity::air_fingerprint()`, which hashes the AIR implementation SOURCES and therefore moves with `circuit/src/descriptor_ir2.rs`. Treat pre-cutover proof identities as non-interoperable. **What was FAIL-OPEN and is now closed in the same commit:** that source closure did not cover the new interpreter or the emitted artifact, so the constraints of the live double-spend gate could have changed while the identity stood still — `circuit/src/table_air.rs` and the JSON are now both `include_bytes!`d into it. **What did NOT change:** the key width. `MA_KEY`/`MA_LO_ADDR`/`MA_LO_NEXT` are still one felt each (2^15.45 collision — the binding figure, the attacker chooses both sides; second-preimage is 2^30.91 and quoting it here would be quoting the flattering number of the pair), so `mapabsent_key_width_tooth.rs`'s tripwires are NOT flipped and must not be. Schema epoch UNCHANGED at 20 — ⚠ OUT-OF-BAND EPOCH BUMP SINCE: `persist/src/lib.rs` reads **21**. A schema bump landed without a row here, so this log no longer reconstructs the epoch history. Flagged 2026-08-01; the bumping commit needs identifying and a row written retroactively — no persisted state re-encodes. **What now refuses to load:** a table-air JSON whose `"kind"` is not `table_air`, whose IR version is not 2, or which reads a column past its declared width (`table_air::parse_table_air`). Verified: `ir2_degree_budget` (the per-table constraint-degree ledger) UNCHANGED including `map_absent`; honest note spend proves+verifies; forged upper/lower bracket and moved post-root each refused by the emitted gates; 358-column mutation sweep through the real evaluator. ⚠ NAMED SEAM, not closed: `emit_descriptors.py::verify_provenance` walks `DESC.iterdir()` + `by-name/` only, so the new `table-airs/` subdirectory is invisible to the provenance stamp — same shape as the hole that let `by-name/predicate-arith.json` drift. Routing `EmitTableAirs.lean` into the drift gate is the first follow-up. | epoch:20 |
| 2026-08-01T13:20:41Z | ember@nextop.local | **RETROACTIVE ROW, written 2026-08-01 — no emit ran; reconstructed from `git log -S` on `persist/src/lib.rs`** | 0323010be936059ba954b6b7c9ece4b577fa6d5d | 6441705e8a4441d28a25fe86e35b69ea86942092 | no | **NO DESCRIPTORS — the missing SCHEMA EPOCH row, 20 -> 21.** The row immediately above this one (`MapAbsent law-#1 cutover`) recorded "Schema epoch UNCHANGED at 20" and then flagged that `persist/src/lib.rs` reads 21, so the log no longer reconstructed the epoch history. The bumping commit is **`6441705e8`** — *"key nonet: an Ed25519 key and its negation packed to ONE octet — all four twins are now the base-2^29 nonet, and the ninth lane still has no column"* — which changed `CANONICAL_STATE_SCHEMA_EPOCH: u64 = 20` to `= 21` in the same commit that repaired `canonical_32_to_felts_8` (it packed `lo \| mid1<<8 \| mid2<<16 \| ((hi & 0x3F) << 24)` — 30 bits over eight lanes — so `& 0x3F` DISCARDED bits 6-7 of bytes 3, 7, ..., 31, and an Ed25519 key and its negation packed to ONE octet). It bumped the epoch without appending here because it ran no emit: the epoch is a Rust constant and only `emit_descriptors.py` writes this file. **What re-genesised at 21:** every persisted `pre_state_hash`/`post_state_hash` and every `TurnReceipt` carrying them (`compress_member` and `canonical_32_to_felts_4` both moved, so the anchor moves on every turn); executor signatures and federation receipt QCs over epoch-20 anchors were NOT migrated. **What did not move:** no chip arity, table, descriptor width, PI count or VK — hence no descriptor row. ⚠ **The gap this exposes is structural, not clerical:** a schema epoch can be bumped by any commit touching one Rust constant, while this log is only ever appended by the emit script, so the two can diverge again tomorrow. Routing a `CANONICAL_STATE_SCHEMA_EPOCH` check into the same gate that guards this log is the follow-up; until then the reconstruction recipe is `git log -S 'CANONICAL_STATE_SCHEMA_EPOCH: u64 = N' -- persist/src/lib.rs`. | epoch:21 |
| 2026-08-01T20:40:00Z | ember@nextop.local | emit-targeted (receipt-log root `iroot`: 1-felt -> 8-lane fold; PRODUCER ONLY, no emit ran) | 2e15966ff2ef1a559c706ec13627acba1bb4f88d | f7f7b860420a9bc790e67ec4b78c3f7994c803bc | no | **NO `circuit/descriptors/*` CHANGED and NO VK ROTATES — `iroot` is witness-carried and no AIR gate constrains its value** (`weldsAt` covers `base+1..11` + `B_CAP_ROOT`; `rotPins` never names `B_IROOT`; the Rust descriptor audit lists it as a fresh absorbed limb, never a digest). ⚑ **THE WOUND.** `turn/src/rotation_witness.rs::iroot` was a ZERO-seeded left-leaning fold with TWO independent ~31-bit waists — `hash_many [root, hash_many [i, hash_bytes rh]]` — and its header cited `mroot_injective` TWICE as the reason it "makes tamper/truncate/extend/reorder all move". Both halves were false: that theorem was DELETED 2026-07-28 (`ed616d24c`) as pigeonhole-false (false premise AND false conclusion, `MMR.lean` s3.25), and `iroot` is a flat chain, not the MMR's bagged peak forest — no theorem about one was ever a theorem about the other. The cheaper waist is the INNER one: `hash_bytes` squeezes a 32-byte receipt hash to one felt, so two DISTINCT receipts are one log entry. **MEASURED: a real collision in 71,133 Poseidon2 evaluations / 0.86 s** (birthday 2^(30.906891/2) = 2^15.45), built into two well-formed 7-entry receipt logs that produce a BYTE-IDENTICAL 32-byte signed state anchor — `turn/tests/iroot_wide_old_admits_new_rejects.rs`, 6/6. **WHERE IT REACHED:** not the classical path (`consensus_ctx` pins `iroot` to a constant, so that anchor never bound the log at all) — the SOVEREIGN path, where `sdk::cipherclerk` folds the agent's real receipt chain in and `executor/atomic.rs:992-993` writes the resulting commitments straight into `TurnReceipt::{pre,post}_state_hash`, which `receipt_hash` absorbs and the executor signs. **WHAT LANDED:** `Dregg2/Lightclient/ReceiptChain8.lean` (NEW, Lean-authored, 19 `#assert_axioms` green) proving `rchain8_binds_or_collides` — equal roots force equal logs OR a TOTAL extractor names a genuine chip collision — with NO `Compress8CR`/`Poseidon2SpongeCR` hypothesis (both refuted at deployed params) and all four residual poles (decidable / dischargeable / refutable / refutes-the-floor). Its Rust byte-twin is `poseidon2::receipt_chain_root_8`: entry `hash_bytes_8`, leaf `A9(i \|\| entry8)`, node `A16(root8 \|\| leaf8)`. **COLLISION 2^123.63** (birthday over p^8, 8*30.906891 = 247.26 bits); the second-preimage figure ~2^247.3 is NOT the governing number and is not quoted. **SCHEMA EPOCH 21 -> 22, RE-GENESIS:** the empty-log root moved from `BabyBear::ZERO` to the domain-tagged `A2[RCE2, 0]`, and `consensus_ctx` pins the empty root, so EVERY anchor on BOTH paths moves; a store stamped 21 REFUSES to load. (`hash_many_8(&[])` was the first spelling and is DEGENERATE — no rate-4 chunk means no permutation, so lanes 0..3 come back literally zero; caught by `iroot_binds_the_whole_log`.) **DELETED, not deprecated:** the narrow fold body. `rotation_witness::iroot` is now a PROJECTION of `iroot8`, so there is one fold and no twin to drift. ⚠ **WHAT IS NOT CLOSED, stated as a live shortfall and not as a later phase:** `B_IROOT` is ONE column and `ROTATED_PADS` is empty, so lanes 1..7 are computed and DISCARDED at the wire — the value reaching the anchor is still one felt at 2^15.45. Named in `rotation_witness::IROOT_LANES_1_TO_7_UNABSORBED`, GATED by `the_residual_is_a_gate_not_a_note` (keyed on `NUM_PRE_LIMBS`, so it goes RED the day the geometry is emitted and forces the projection to be deleted). The cutover: extend the block to carry ir1..ir7 (`Legal.bodyAligned` forces extent 196, not 195), change the WIDE final chain site from arity 11 (`d8 \|\| iroot \|\| 0 \|\| 0`, `EffectVmEmitRotationWide.lean:217`) to arity 16 (`d8 \|\| iroot8` — already an admitted arity), split the NARROW V3 final site (`EffectVmEmitRotationV3.lean:500`) off arity 9 which is NOT admitted, re-emit, rotate the VK, bump to 23. **BLOCKED, verified at source and not relayed:** `lake env lean --run EmitLayoutManifest.lean` fails to LOAD — `EffectVmEmitRotationWide.olean` does not exist because that module is RED under another lane's in-flight 187 key-nonet flag day — so `emit_descriptors.py` cannot run at all, and the extent change would clobber that lane's edit. **ALSO STALE, same class, NOT fixed here:** `dregg-query/src/mmr.rs:10,302`, `dregg-query/tests/synthetic.rs:277` and `grain-turn/src/lib.rs:144` each still cite `mroot_injective`; that crate's MMR is BLAKE3/32-byte and is NOT width-wounded, so it is a citation repair, not a fold repair. | epoch:22 |
| 2026-08-01T21:56:48Z | ember@lunar.town | classify-and-migrate (the `hash_bytes` CALLER sweep: every production caller classified A/B/C; the two migratable (A) sites widened; NO emit ran) | fc44a679fddc9057d20e2d452a5b23f60ca78650 | 5bfdb4d985e8972f8b9e8d8609dd33e2dc4ab828 | YES | **NO `circuit/descriptors/*` CHANGED AND NO VK ROTATES — and that is a MEASURED finding, not an assumption.** `hash_bytes`' SQUEEZE has no in-AIR counterpart anywhere in the tree: `metatheory/Dregg2/Circuit/BytesLanes.lean` stops at the byte→lane PREIMAGE and defines no sponge at all, every Lean hash carrier is felt-/`Nat`-domain (`hash : List ℤ → ℤ`), the "in-AIR `hash_bytes` recompute" named at `circuit/src/effect_vm/authority_digest_weld.rs:57` is an arity-2 felt-domain chip lookup over two floor felts in a gadget with ZERO production callers (`InAirAuthorityDigestSelector.lean:42` calls the byte-sponge version *"the named, genuinely-VK-affecting remaining work"* — not built), and the `fields_root` anchor prose at `EffectVmEmitRotationV3.lean:4765/4838/4922/4945` is **STALE**: the deployed `cell::commitment::fields_root_felt` is a byte-decode and PI 46 is anchored with the BLAKE3 `compute_authority_digest_felt`. ⚑ **THE SCOPE.** 306 grep hits over 59 files reduce to **~10 production files** calling `dregg_circuit::poseidon2::hash_bytes`; the rest are HOMONYMS (`dungeon-on-dregg`/`dreggnet-offerings`/`fhegg-fhe` each define a LOCAL `fn hash_bytes` over blake3; `node/src/api.rs`, `turn/src/conflict.rs`, `sdk/src/cipherclerk.rs` have local VARIABLES named `*_hash_bytes`). **(A) MIGRATED — 1: the zkOracle cross-leg weld.** `zkoracle-prove::attestation::content_commitment` and `render::template_commitment` return `[BabyBear; 8]`; `ZkOracleAttestation::content_commit` and `RenderAttestation::{output_commit, template_commit}` widen with them. The felt was the ONLY binding at `dungeon_on_dregg::narrator::attestation_commit_field` → `data[SLOT_ATTESTATION_COMMIT]` of the narration `EmitEvent`, inside a committed chain-linked `TurnReceipt`, with `RecordedNarration::attestation_commit` compared by VALUE EQUALITY because the attested body is not retained — felt-width wound site #18, now closed. On the LIVE Bedrock path the envelope's `metrics.latencyMs`/`usage`/`stopReason` are not determined by the narration text, so `data[0]` (the wide BLAKE3 narration commitment) agrees by construction and `data[1]` carried the whole discriminating burden. **MEASURED: a real collision in 41,522 Poseidon2 evaluations / 3.27 s** over two genuinely attestable Bedrock-shaped bodies carrying one narration, producing a BYTE-IDENTICAL narration event under the real `dregg_turn::absorb_emitted_event` — `dungeon-on-dregg/tests/attestation_commit_wide_old_admits_new_rejects.rs`, 5/5, and the binding tooth's own `expected_narration_event_data` separates the pair only after the widening. **(A) MIGRATED — 2: the Poseidon2 hash-LOCK (the epoch bump).** `cell::program::eval::hash_preimage32`, the shared digest behind `StateConstraint::{PreimageGate, KeyRotationGate}`, used `felt_to_bytes32(hash_bytes(p))` while the `HashKind::Blake3` arm in the SAME `match` used all 32 bytes. `PreimageGate` is a hash-lock: a second preimage of a published commitment costs `2^30.91` ≈ 1.5e9 evaluations (hours on one core) and the opener never learns the real secret; `KeyRotationGate` is worse because the forged preimage is **INSTALLED** as the cell's current key set. **MEASURED: a real collision in 67,066 evaluations / 0.82 s**, the retired predicate admitting a foreign 32-byte key set and the deployed evaluator refusing it — `cell/tests/preimage_gate_wide_old_admits_new_rejects.rs`, 5/5. Now `digest8_to_bytes32(hash_bytes_8(p))`: collision `2^123.63`, second preimage `2^247.26` (COLLISION is the governing figure for the equivocation, second-preimage for the attack on someone else's lock — both quoted where each applies, neither substituted for the other). **SCHEMA EPOCH 22 → 23, RE-GENESIS:** every persisted `CellState.fields[i]` holding a Poseidon2 `PreimageGate`/`KeyRotationGate` digest, and every state commitment over such a cell. A store stamped 22 REFUSES to load; an un-re-genesised slot is fail-CLOSED (unopenable), never silently narrow. `HashKind::Blake3` slots untouched. **OUTSIDE THE LEDGER, same commit:** every serialized `ZkOracleAttestation`/`RenderAttestation` (the serde shape moved from a scalar to an 8-array, so an epoch-22 attestation REFUSES to deserialize rather than reinterpreting), every `attested-dm`/`deos-hermes` `attestation_commitment` receipt id (both now absorb all eight lanes — a wide BLAKE3 seeded from a ~31-bit value inherits that value's 2^15.45 collision set exactly), and every `dungeon-on-dregg` narration receipt that bound an attestation. **(B) LEFT, COMPANION LOCATION WRITTEN INTO THE CALL SITE:** `storage::bucket_commitment::fold_leaves` and `starbridge-apps/site-host`'s `content_root` each take a 1-felt `coll = hash_bytes(key)`, and each has the key bound BYTE-EXACTLY beside it (`object_leaf`/`asset_digest8`, length-prefixed + 3-bytes-per-felt injective packing, whose limbs ride into `wire_commit_8`'s `pre`) — so the published root separates at 2^123.63 and `coll` reaches only the `iroot` argument. ⚠ Both comments state the condition that would flip them to (A): serving a MERKLE PATH through that heap instead of the full leaf list. **(C) LEFT:** the two `hash_bytes(DOMAIN)` tags — compile-time constants with no free parameter, so no search exists at any price and the birthday figure does not apply. **(A) NAMED, NOT MIGRATED, each at its own definition with the cost and the owner:** `exec_lean::nullifier::addr_of` (the `MapAbsent` IMT key — `MA_KEY`/`MA_LO_ADDR`/`MA_LO_NEXT` are one felt each in the deployed `noteSpendVmDescriptor2R24`, so this is **Lean emit work**, emitters already proved and unlanded at `Emit/LexCompare8Emit.lean` + `Emit/HeapLeafWideEmit.lean` + `metatheory/Dregg2/Circuit/MapAbsentImtGateWide.lean`; ⚑ and it needs the base-2^29 NONET, not eight lanes, because a KEY needs injectivity over 256 bits and 8 lanes carry 247.26 — pigeonhole, the 2^123.63 hash-NODE figure does NOT transfer to a key); `wasm`'s `fact_hash` → column `PREDICATE_SYM` of the deployed `predicate-arith*` goldens (a descriptor reshape; also `wasm` does not compile at HEAD, a sibling lane owns it); `sandstorm_bridge::cell::{var_addr, var_value_felt}` (whose whole `/var` ROOT is one felt, so widening only the leaves would be a containment below the bar — the fix is the tree, stated as one unit); and the sel4 `crypto-floor` portal returns (⚠ `dreggcf_keyed_mac` hands back a **~31-bit MAC tag** for a macaroon caveat chain — the old docstring's "a full 256-bit digest does not fit a Nat scalar" is FALSE, a Lean `Nat` is unbounded and the cap is the `uint64_t` C ABI). ⚠ **ALSO CORRECTED:** `circuit-prove/src/zkoracle_leaf_adapter.rs`'s header claimed its 1-felt exposed lane was "the same width `content_commitment` itself has" — no longer true, and the gap now runs the other way (the PI-pinned in-AIR leaf is the narrow one; widening it is a PI-count change). | epoch:22 |
| 2026-08-01T23:39:56Z | ember@nextop.local | stamp-existing (⚑ PHASE 3 of the logic compiler: TEN by-name descriptors RE-EMITTED from the compiler into the new canonical normal form) | a6c5129cd8602979fd5acc5a65cb88890c6e08fa | 8827eba0f557494b1c63c12b818dcc85e4ea01bb | YES | **WHAT RE-EMITS — ten `circuit/descriptors/by-name/*.json`**: merkle-membership-depth2, attested-fact-membership, blinded-membership, presentation-freshness, predicate-arith (ge), predicate-arith-gt, predicate-arith-le, predicate-arith-lt, predicate-arith-neq, predicate-arith-inrange — plus their ten `by_name_sha256` rows here and TWO Rust `GOLDEN_JSON` literals that embed bytes verbatim (`circuit-prove/tests/merkle_membership_emit_gate.rs`, `circuit-prove/tests/presentation_emit_gate.rs`). ⚑ **WHY**: each descriptor's `EffectVmDescriptor2` is now literally `EffectLower.lowerAir` of an `EffectAir` source and its hand-written `VmConstraint2` list literal is DELETED (commit `8827eba0f`, Phase 3 of `metatheory/docs/LOGIC-COMPILER-ASSESSMENT.md`). The compiler renders every term with its coefficient, so a unit coefficient that was written bare (`{"t":"var","v":5}`) is now `mul(const 1, x)`. **THE POLYNOMIAL IS UNCHANGED** — `AirNormalForm.gateBody_eval` proves the compiled body IS `lhs − rhs`, and every `_zero_iff` obligation is re-established VERBATIM over the compiled object. ⚑ **NO VK ROTATES, and that was CHECKED not assumed**: none of the ten is a member of `rotation-v3-staged-registry.tsv` (they are the `descriptor_by_name` predicate-dispatch surface), no `*_FP` constant in any of the five `RUST_FP_FILES` names any of them, and the p3 symbolic degree is identical (`LeanExpr::degree` is `Const => 0`, `Mul => sum`, so `mul(const 1, x)` is degree 1 exactly as bare `x` is; `Ir2Air::Main::max_constraint_degree` returns `None` and infers from that same algebra), so the FRI blowup does not move. **A REBUILD, NOT A RE-GENESIS. Schema epoch UNCHANGED at 23.** **WHAT NOW REFUSES TO LOAD**: nothing silently — a stale copy of any of the ten reds `scripts/check-descriptor-drift.sh` (Lean re-derives and diffs) and `--verify-provenance` (sha mismatch), both by name. ⚑ **ALSO STAMPED, a pre-existing coverage gap closed in passing**: the six tracked `circuit/descriptors/table-airs/*.json` (the TableAir lane's artifacts) had NO row in this stamp at all — `verify_provenance` could not see them, the exact shape of the hole that let `by-name/predicate-arith.json` drift. They are covered now. ⚠ **`source_dirty=true`, eyes-open**: `metatheory/Dregg2` carries several sibling lanes' in-flight edits in the shared tree, so `DREGG_VK_REGEN_ALLOW_DIRTY=1` was required; the ten descriptor bodies themselves are NOT transcriptions — each is byte-pinned by a machine-checked `#guard emitVmJson2 <desc> == "…"` in its authoring module, `lake build` GREEN over 3047 targets on hbox. ⚠ **NOT RUN**: the full `scripts/check-descriptor-drift.sh` (a ~1h Lean corpus build plus rustfmt) — the ten Lean byte-pins and the file-vs-pin equality were verified directly instead. | epoch:22 |
| 2026-08-01T23:42:23Z | ember@nextop.local | stamp-existing (RE-STAMP of the row above, after repairing the driver) | a6c5129cd8602979fd5acc5a65cb88890c6e08fa | 8827eba0f557494b1c63c12b818dcc85e4ea01bb | YES | **Same ten descriptors as the row above; this row exists because the FIRST stamp could not be VERIFIED.** ⚑ A WRITE/VERIFY ASYMMETRY IN THIS DRIVER, found by running its own gate: `verify_provenance` had been taught (2026-08-01, the `table-airs/` landing) to walk EVERY subdirectory of `circuit/descriptors/` by DISCOVERY, but `build_provenance` still special-cased `by-name/` alone — so the six tracked `circuit/descriptors/table-airs/*.json` were written into `descriptor_sha256` under a slash-keyed name while `table-airs_sha256` came out **null**, and `--verify-provenance` reported them BOTH ways at once (`recorded in the stamp but MISSING on disk` from the flat walk, and `on disk but NOT covered by the stamp` from the discovery walk). A stamp that cannot be verified attests nothing. `scripts/emit_descriptors.py` now derives the subdirectory legs by discovery on the WRITE side too (`subdir_hash_legs`), symmetric with the verify side, so a future descriptor subdirectory is stamped the day it appears. `--verify-provenance` now **PASSES** — 147 descriptor files + 5 FP files. No descriptor byte changed between the two rows. | epoch:22 |
| 2026-08-02T00:42:52Z | ember@nextop.local | stamp-existing | 2475534ae633f0a0ac1e7a8ca9583dfdc4f09ea3 | 43a95316e214153fe04a4f197b663f9818e1f5a7 | YES | **NO `circuit/descriptors/by-name/*.json` CHANGED — no descriptor re-emits, no PI count, no trace width.** What changed is the AIR IMPLEMENTATION: `Ir2Air::UMemory` — the OP LOG of the ONE Blum multiset over `Domain x kappa`, i.e. five booleans, the real-row prefix, the positional serial chain, a read discipline over BOTH components of the `Option` cell, canonical-`none` on both images, the 30-bit serial-gap range check, the NULLIFIER insert-only tooth, the `ir2_umem_log` receive, both `ir2_umem_check` Blum legs and the `ir2_umem_addrs` closure query — is **DELETED (the enum VARIANT, not just its body)** and replaced by a seventh `Ir2Air::LeanTable` instance walking a NEW artifact, `circuit/descriptors/table-airs/dregg-ir2-umemory-v1.json` (NEW, 5,773 bytes), emitted by `metatheory/Dregg2/Circuit/Emit/UMemoryTableEmit.lean` and byte-pinned there by `#guard UMEMORY_JSON.length == 5772`. 20 gates + 12 bus interactions at width 23; `ir2_degree_budget` UNCHANGED for `umemory` (3). **WHAT ROTATES:** `faithful_note_spend_exact_v3_identity::air_fingerprint()` and `verifier_source_closure_fingerprint()` — both hash `circuit/src/descriptor_ir2.rs`, `circuit/src/table_air.rs` and `TABLE_AIR_ARTIFACTS` spliced whole, and all three moved. Treat pre-cutover proof identities as non-interoperable. **A REBUILD, NOT A RE-GENESIS. Schema epoch UNCHANGED at 23** — no persisted shape moved. **ALSO LANDED, and it is the half that had to go first:** the row-local sweep oracle (`descriptor_ir2::table_air_gates_accept`) built an EMPTY preprocessed window for every AIR, so `Ir2Air::ExactPublicTable` — which reads NOTHING BUT preprocessed columns — was unreachable by any mutation sweep in the repo (`TableAirIR` §7 item 4). The window is now real and the new `ir2_air_gates_accept` **REFUSES** a shape mismatch instead of substituting zeros, keyed on the AIR's own `BaseAir::preprocessed_width`. `circuit/tests/ir2_preprocessed_sweep_harness.rs` measures both poles. **WHAT NOW REFUSES TO LOAD:** unchanged from the six earlier table-air landings — a table-air JSON whose `"kind"` is not `table_air`, whose IR version is not 2, or which reads a column past its declared width. ⚠ **WHAT THE PORT REFUTED, and there are two:** (1) *"prev_serial < serial (Disciplined), exactly the flat memory's gap shape"* — the second clause is TRUE and it IS the refutation; there is no magnitude gate on `UM_PREV_SERIAL`, so a nullifier-domain freshness read at serial 1 may claim a prior serial of p-5 and satisfy every gate. (2) *"a nullifier-domain write installing `none` is UNSAT"* — true of REAL rows only; the tooth carries no `is_real` factor and a PAD row spelling the forbidden op is green. Both containments are the `is_real`-gated bus legs, one object out. ⚠ `source_dirty=true`, eyes-open: `metatheory/Dregg2` and `circuit/src/poseidon2.rs` carry sibling lanes' in-flight edits in the shared tree, so `DREGG_VK_REGEN_ALLOW_DIRTY=1` was required; NO descriptor byte outside the new table-air artifact changed (`git status circuit/descriptors/` showed exactly one added file before the stamp). | epoch:22 |
| 2026-08-02T01:38:38Z | ember@nextop.local | emit | 63dc4fa6ce52c2d9c53783da7291ceec2d41a7f6 | 86bd9ce18c70eb3abf828c9a5af1ece597ac61cd | YES | by-name/faithful-note-spend-exact-v3.json, rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs, circuit/src/effect_vm/e1_compact_generated.rs, circuit/src/effect_vm/s2_compact_generated.rs, … +4 | epoch:22 |
| 2026-08-02T14:39:55Z | agent@nextop.local | stamp-targeted (MapOps law-#1 cutover: the map RECONCILIATION table's AIR moves from hand-written Rust to a Lean emission) | 1123c3d021ca8f0c7460544fce425a024208e613 | (this commit) | YES | **NO `circuit/descriptors/by-name/*.json` CHANGED — no descriptor re-emits, no PI count, no trace width.** What changed is the AIR IMPLEMENTATION: `Ir2Air::MapOps` — the map reconciliation table, i.e. the row guard and op membership, the AAFI selector's three pins, the read discipline, 32 direction booleans across TWO independent paths, the pointer-bracket range block (three canonical splits + two lexicographic comparators) and FIVE `node8` Merkle folds totalling 84 chip lookups — is **DELETED (the enum VARIANT, not just its ~320-line body)** and replaced by an eighth `Ir2Air::LeanTable` instance walking a NEW artifact, `circuit/descriptors/table-airs/dregg-ir2-map-ops-v1.json` (NEW, 338,976 bytes, sha256 `acf57179bd6d50abde7cb110a181a4b6f3f1db0b509c6ee7fb87a960555e063b` — the LARGEST of the eight), emitted by `metatheory/Dregg2/Circuit/Emit/MapOpsTableEmit.lean` and byte-pinned there by `#guard MAP_OPS_JSON.length == 338975`. 91 gates + 120 bus interactions at width **898**; `ir2_degree_budget` UNCHANGED for `map_ops` (4). **WHAT ROTATES:** `faithful_note_spend_exact_v3_identity::air_fingerprint()` and `verifier_source_closure_fingerprint()` — both hash `circuit/src/descriptor_ir2.rs`, `circuit/src/table_air.rs` and `TABLE_AIR_ARTIFACTS` spliced whole, and all three moved (`TABLE_AIR_ARTIFACTS[7]` added to BOTH splice lists). Treat pre-cutover proof identities as non-interoperable. **A REBUILD, NOT A RE-GENESIS. Schema epoch UNCHANGED at 23** — no persisted shape moved. **WHAT NOW REFUSES TO LOAD:** unchanged from the seven earlier table-air landings — a table-air JSON whose `"kind"` is not `table_air`, whose IR version is not 2, or which reads a column past its declared width (`table_air::parse_table_air`). ⚠ **WHAT THE PORT REFUTED, and it is the load-bearing half TWICE.** (1) `an_aafi_row_with_no_opening_satisfies_every_gate` — **NOT ONE of this table's 84 chip legs is a gate.** A row with `root = new_root = 0`, an honest pointer bracket and an ALL-ZERO opening (no leaf digest, no sibling, no chain node) satisfies every one of the 91 EMITTED gates at `is_real = 1`, proved by `decide` over `RowHolds` on the emission itself and measured again at the deployed evaluator (`an_opening_free_row_is_accepted_by_every_emitted_gate`). The Merkle content of this table is 70% of its interactions and 0% of its algebra; what refuses a forged opening is the `ir2_p2` chip bus in a DIFFERENT AIR. (2) `a_pad_row_can_spell_an_aafi_insert` — the deleted arm's *"all ride `aafi_gate = is_real*is_aafi` ... so op<=3 rows see NONE of these lookups/asserts"* is true in its op<=3 half and NOT in its `is_real` half: the 16 PATH2 direction booleans, the 8 empty-slot pins and all 45 range-block gates carry **`s` alone**, so a PAD row may spell a complete AAFI insert, still owes a REAL bracket, and still SENDS the range block's 35 byte queries at multiplicity 1 while every Merkle opening is off. The gates are STRICTER on a pad than the comment suggests and the bus legs are WEAKER. **ALSO MEASURED:** the read-discipline gate is UNREACHABLE from the prover entry point — `build_traces` writes `MAP_OLD_VALUE` from the heap, so the ASSEMBLER refuses a lying read first (`"map op 0: read at key 100 opens to 77, row claims 99"`), which is a completeness-of-refusal fact and NOT a gate verdict; `assert_violated_constraint_not_bus` is deliberately not applied to it, and the gate's own verdict is measured on a MUTATED assembled read row instead. ⓘ **THREE CORRECTIONS TO THE RUST, none of which broke anything:** (a) the trailing comments on the `MAP_*` layout block were STALE by one from `MAP_DIR0` (`// 149`) through `MAP_WIDTH` (`// 897`) — the arithmetic gives 150 ... **898**, the drift being the `MAP_NEXT` column the arity-2 -> arity-3 IMT move inserted; the Rust shape test now pins `t.width == MAP_WIDTH` so the two sources must agree. (b) `eval_canon_decomp` and `eval_lex_lt` had been DEAD since the MapAbsent cutover on 2026-08-01 and were still counting against law #1 — 20 of the 32 baseline sites this pass retires are theirs and the three GATE-COUNTED twins'. (c) `TableAirIR` gained §6c (`group8`/`chipAbsorbTuple`/`node8Tuple`/`foldQueries` + `fold_is_a_chain`), HOISTED out of `MapAbsentTableEmit`; map-absent's artifact re-emits **BYTE-IDENTICAL** (sha256 `e619e2b7...` unchanged, verified by regenerating all eight and diffing). **law #1 baseline for `circuit/src/descriptor_ir2.rs`: 236 -> 204**, re-pinned to the measurement. ⚠ `source_dirty=true`, eyes-open: `circuit/src/effect_vm_descriptors.rs` carries a sibling lane's in-flight edit and was deliberately NOT re-stamped; the ONLY provenance row this commit writes is `table-airs_sha256["dregg-ir2-map-ops-v1.json"]`. | epoch:22 |
| 2026-08-02T14:52:13Z | agent:key-nonet-ninth-lane | producer-cutover (NO descriptor re-emit — the 187 geometry and the ninth-lane columns were ALREADY emitted at `606656cd0`; this row is the PRODUCERS catching up to them) | 20602847d6daf693312bc4d6cda0e944ab7cdff6 | b6f1de898e5cdb70f5e48b7d1591b2c99381f7a6 | YES | **THE OWNER KEY'S NINTH LANE REACHES THE SIGNED ANCHOR.** `cell/src/commitment.rs::compute_rotated_pre_limbs` and `turn/src/rotation_witness.rs::produce` wrote the base-`2^29` nonet's LOW EIGHT lanes to `B_PUBKEY_OCTET` and dropped lane 8, because the 184-limb geometry had no column for it. Both now write all nine through `Faithful9::from_key_lanes9` over `dregg_circuit::effect_vm::PUBKEY_NONET_LANE_COL` (105..=112 ‖ 186). `Faithful8::from_key_nonet_low8` and `KEY_NONET_NINTH_LANE_UNBOUND` DELETED with their three burn-down rows in `docs/FAITHFUL-COMMITMENT-LAW.md`, which is now EMPTY. `circuit/src/effect_vm/helpers.rs` gains `key_limbs9` / `key_from_lanes9` (the inline nonet in `trace.rs` HOISTED — not a fifth twin) and `PUBKEY_NONET_LANE_COL`; `Faithful9` gains `from_key_lanes9` / `to_key_bytes`. FLIPPED, and said out loud: `key_nonet_ninth_lane_reaches_the_anchor.rs::the_geometry_is_still_at_184_and_here_is_what_that_costs` became `the_geometry_reached_187_and_every_lane_has_a_column`. NEW exhibit: `key_nonet_anchor_old_admits_new_rejects.rs`. ⚠ **CORRECTION TO THE FRAMING — measured on the first run, not relayed:** the anchor did NOT bind only 232 of 256 key bits. `authority_residue_bytes` absorbs `cell.public_key` BYTE-EXACTLY into `AUTHORITY_DIGEST_GROUP` (limbs 24 ‖ 12..=18), so `state_commit` ALREADY separated `A` from `-A`, at blake3-into-`from_bytes32` strength (the law's 2^123.63 collision figure). What sat at 232 bits — and merged `A` with `-A` at cost ZERO — is the dedicated KEY CARRIER that the KEY_COMMIT teeth and the octet welds read. The upgrade is **232 bits -> an INJECTION on the key's own columns**, not 0 -> 256 at the anchor. ⚠ **NOT closed by this row:** `Emit.KeyCanonicity9Emit.keyCanonical9At` (the canonicity envelope — 8 lookups at 29 bits plus 1 at 24) is APPLIED BY NOTHING, so the anchor BINDS the nonet and nothing REFUSES an out-of-image lane vector; and `B_CHILD_VK_NINTH_LANE` / `B_CONTRACT_HASH_NINTH_LANE` (184, 185) are emitted columns that no producer writes, while `Faithful8::from_bytes32` is O(1)-aliasable for the attacker-chosen 32 bytes those two carry. | epoch:22 |
| 2026-08-03T00:25:42Z | ember@nextop.local | emit | 61eb1435100c747c4f609a20e934cb055dec87cc | 9ea58eff160e65c3dff0ffc7cb9cd554ba02c57f | no | rotation-v3-staged-registry.tsv, rotation-wide-registry-staged.tsv, table-airs/dregg-ir2-byte-v1.json, table-airs/dregg-ir2-chip-state16-v1.json, table-airs/dregg-ir2-chip-v1.json, table-airs/dregg-ir2-map-absent-v1.json, … +20 | epoch:22 |
| 2026-08-03T00:32:50Z | ember@nextop.local | stamp-existing | 61eb1435100c747c4f609a20e934cb055dec87cc | b221dcff8eb3770191b3800c16656317034380cc | no | (stamp only) | epoch:22 |
| 2026-08-03T00:38:23Z | ember@nextop.local | stamp-existing | 5cd4546658fca5fe25734cb8156647ce0ae70e72 | 0a24ea9940839994bae73949f5789e391cfb5102 | no | (stamp only) | epoch:22 |
| 2026-08-03T02:32:30Z | ember@nextop.local | emit | 79a720f05654d00d16ea5a238043acb306e21449 | 36e80c2dbf4950cb9000a0102ee849b7d5b80696 | no | PROVENANCE.json: table-airs_sha256: `dregg-ir2-exact-public-v1.json` was emitted but has NO row in the stamp, PROVENANCE.json: table-airs_sha256: `dregg-ir2-byte-v1.json` pin does not equal the emitted bytes, PROVENANCE.json: table-airs_sha256: `dregg-ir2-chip-state16-v1.json` pin does not equal the emitted bytes, PROVENANCE.json: table-airs_sha256: `dregg-ir2-chip-v1.json` pin does not equal the emitted bytes, PROVENANCE.json: table-airs_sha256: `dregg-ir2-map-absent-v1.json` pin does not equal the emitted bytes, PROVENANCE.json: table-airs_sha256: `dregg-ir2-map-ops-v1.json` pin does not equal the emitted bytes, … +5 | epoch:22 |
| 2026-08-03T13:52:00Z | ember@nextop.local | classify-and-migrate (the grain `/var` COMMITMENT widened as one unit; NO emit ran, NO descriptor changed, NO VK rotates) | 9b8a277fbc8613972bac7f834b5114a338ba208d | cd370a02eb8294e3830a692169dffa0a375fad2e | YES | **NO `circuit/descriptors/*` CHANGED AND NO VK ROTATES — the opening this site serves is the native `sandstorm_bridge::cell::verify_inclusion`, not a STARK, so nothing here reaches an emitted AIR.** ⚑ THE SITE: `sandstorm_bridge::cell` committed ONE `HeapLeaf` per `/var` entry — `addr = var_addr(key)`, `value = var_value_felt(value)`, root = `felt_to_bytes32(compute_heap_root(..))`, every one a single BabyBear lane. `var_value_felt` was the ONLY binding of an arbitrary-length served card, and it is exactly what `verify_inclusion` recomputes for a visitor holding a ledger-obtained root. **MEASURED: a real collision in 78,272 Poseidon2 evaluations / 5.20 s (debug)** over two genuinely well-formed grain settings cards (`{"theme":"dark","tz":"UTC","visits":45393}` vs `visits=78271`), with the RETIRED construction reproduced in the test and driven END TO END: the honest root ACCEPTS the swapped card, and the retired `data_root` does not move either, so the ledger anchor could not see the swap — `sandstorm-bridge/tests/var_leaf_wide_old_admits_new_rejects.rs`, 4/4, 91/91 crate-wide. ⚑ **MIGRATED AS ONE UNIT, because the ROOT was one felt too** — widening only the leaf under a 2^15.45 root would have been a containment below this repo's own bar, and quoting 2^123.63 for a leaf under it would have been the flattering half of a pair. `Umem` now commits through `CanonicalHeapTree8` / `compute_canonical_heap_root_8` (every node a `heap_node8`, every leaf a `digest8`) encoded by `digest8_to_bytes32` — the same node8 scheme `dregg_cell::state::compute_heap_root` uses. ONE ENTRY IS NOW EIGHT LEAVES: a `HeapLeaf`'s `value` is one felt regardless of tree width, so the repair is the spread `storage::bucket_commitment::fold_leaves`' own (B) note PRESCRIBES for a site that serves a MERKLE PATH — lane `i` = `(heap_addr(var_coll(key), i), var_leaf_digest8(key, value)[i])`. The KEY is bound at full width INSIDE the leaf values (`DOMAIN ‖ len(key) ‖ key ‖ value` through `hash_bytes_8`), which is what makes the surviving one-felt `var_coll` non-load-bearing; the condition that would flip it back is written at its definition. **BOUND, DERIVED — COLLISION, not second preimage**, because a hostile host chooses BOTH sides: 8 · 30.906891 = 247.2551 bits ⇒ **2^123.6276** collision (2^247.2551 second preimage). Retired: 2^15.4534 / 2^30.9069. The test computes both from `p` and `VAR_LANES` at run time. ANTI-VACUITY: honest cards still verify at every step; each of the eight lanes is shown load-bearing by corrupting it in turn; and the fold is shown to move for the key, the value, the value LENGTH and the key-length prefix's split point. **RE-GENESIS:** every grain `data_root` / checkpoint wire string (prefix `heap1…` → `heap8…`, so a stored v1 backup compares UNEQUAL and `grain::restore` REFUSES rather than reinterpreting), and every `InclusionProof` (eight `LaneOpening`s with 8-felt paths, not one 1-felt path). `cell::{var_addr, var_value_felt, felt_to_bytes32}` are DELETED, not deprecated — the narrow shape is a compile error; domain tags `:v1` → `:v2`. **NO SCHEMA-EPOCH MOVE:** no `CellState` shape changes, so `CANONICAL_STATE_SCHEMA_EPOCH` is untouched by this commit; `persist/src/lib.rs`'s epoch-19 note named the two deleted functions and now names their replacements. ⚠ **LEFT OPEN, WITH ITS PRICE:** `var_coll` is still one felt, so two `/var` keys can share all eight lane addresses at 2^15.4534 — NOT a forgery (the key is bound again inside the leaf values) but an AVAILABILITY event, `assert_addr_unique` PANICS fail-closed. The eight-fold leaf count moves it: likely near ~5,793 entries rather than ~46,341, and the depth-16 tree now holds 8191 entries rather than 65535. Closing it needs an address wider than one felt = `HeapLeaf`'s shape = the same Lean/emit item named at `exec_lean::nullifier::addr_of`. | epoch:unchanged |
| 2026-08-03T18:05:00Z | ember@nextop.local | record-only (NO emit, NO re-genesis — reconciling the log with a bump that landed between two lanes) | 4e4b0226d182ca0501bd76839d243ea330e54db2 | 3dcefe00a742f4a2b5f1205cb53f77a6ba25fa8b | false | (none) | epoch:23 |
| 2026-08-05T18:24:25Z | ember@nextop.local | emit | 859d99945b9f00f7ed0a854ce93b1131d8abdf76 | 0e52bee2d2991c097c5c21204b03f00e13cb37f5 | YES | by-name/accumulator-nonrev.json, by-name/adjacency-membership-wide.json, by-name/attested-fact-membership.json, by-name/automatafl-legc-n11.json, by-name/automatafl-legc-n5.json, by-name/automatafl-resolve-marks-n11.json, … +278 | epoch:23 |
| 2026-08-05T18:40:00Z | ember@nextop.local | record-only (correcting the row ABOVE: its 140-file INSTALL landed and stands; its PROVENANCE.json STAMP was deliberately REVERTED and PROVENANCE.json is unchanged from the 2026-08-03T02:32:30Z stamp) | 859d99945b9f00f7ed0a854ce93b1131d8abdf76 | 0e52bee2d2991c097c5c21204b03f00e13cb37f5 | YES | ⚑ THE INSTALL IS REAL, THE STAMP IS NOT. `emit_descriptors.py` installs and stamps in one step and there is no install-without-stamp path, so the run above wrote both; the stamp was then restored to its previous bytes and only the descriptor install kept. WHY: `metatheory/Dregg2` was DIRTY (five lanes live), so the stamp it minted recorded `source_dirty=true` — a stamp `--verify-provenance` REFUSES outright. Taking it would have replaced a truthful "26 by-name artifacts carry no stamp row" signal with a blanket "this stamp is invalid", and would have manufactured a provenance CLAIM about a Lean tree nobody reviewed. The honest state is: the served bytes ARE the Lean emission (one run, `EmitByName.lean` + eleven newly-routed rows), and NOTHING attests them yet. WHAT A STAMP WOULD NOW COVER, exactly: `by_name_sha256` 76 → 102 rows (26 NEW: the 11 formerly hand-driven `mina-commit-*`/`mina-xi-*`/`pasta-fq-*` + `dark-bazaar-private-poa-settlement-n4k4-v4` + `dregg-mina-lightclient-{verify,link}-v1` + `dregg-solana-stake-table-fold-v1` + `pasta-alu-{,fq-}sound` + `pasta-sbox-prog{,-1k}` + `pasta-fq-chainlink` + the six `pasta-f{p,q}{mul,add,sub}-sound`) and 76 MOVED hashes; `descriptor_sha256` 40 MOVED of 71; `fp_file_sha256` 2 MOVED (`circuit/src/effect_vm_descriptors.rs`, `circuit/src/lean_descriptor_air.rs`); `table-airs_sha256` unchanged. 144 rows added-or-moved in total. ⚠ The ceremony wants a quiet tree: commit the five lanes' `metatheory/Dregg2` edits first, then `DREGG_VK_REGEN_ACK="$(git rev-parse HEAD:metatheory/Dregg2)" scripts/emit-descriptors.sh` with NO `ALLOW_DIRTY`, which is a NO-OP install plus a clean stamp. | epoch:23 |
| 2026-08-06T00:04:01Z | ember@nextop.local | emit | 797331ff5c326021933173e951059c9a1c8995cd | e712723e1beedc1cadb4c30aaed334a9fc8a0006 | YES | by-name/dregg-mina-lightclient-verify-v1.json, PROVENANCE.json: descriptor_sha256: `dregg-bilateral-aggregation-v3.json` pin does not equal the emitted bytes, PROVENANCE.json: descriptor_sha256: `dregg-bundle-tree-fold-v2.json` pin does not equal the emitted bytes, PROVENANCE.json: descriptor_sha256: `dregg-cert-f-ir2.json` pin does not equal the emitted bytes, PROVENANCE.json: descriptor_sha256: `dregg-cert-f-market4-ir2.json` pin does not equal the emitted bytes, PROVENANCE.json: descriptor_sha256: `dregg-cross-cell-conservation-v2.json` pin does not equal the emitted bytes, … +144 | epoch:23 |
| 2026-08-06T00:07:17Z | ember@nextop.local | emit | 797331ff5c326021933173e951059c9a1c8995cd | e712723e1beedc1cadb4c30aaed334a9fc8a0006 | YES | by-name/mina-wrap-conjunction-unthreaded.json, by-name/mina-wrap-conjunction.json, PROVENANCE.json: descriptor_sha256: `dregg-bilateral-aggregation-v3.json` pin does not equal the emitted bytes, PROVENANCE.json: descriptor_sha256: `dregg-bundle-tree-fold-v2.json` pin does not equal the emitted bytes, PROVENANCE.json: descriptor_sha256: `dregg-cert-f-ir2.json` pin does not equal the emitted bytes, PROVENANCE.json: descriptor_sha256: `dregg-cert-f-market4-ir2.json` pin does not equal the emitted bytes, … +147 | epoch:23 |
| 2026-08-06T00:10:00Z | ember@nextop.local | record-only (correcting the row ABOVE: its ONE-FILE INSTALL landed and stands; its PROVENANCE.json STAMP was deliberately REVERTED and PROVENANCE.json is unchanged from the 2026-08-03T02:32:30Z stamp) | 797331ff5c326021933173e951059c9a1c8995cd | e712723e1beedc1cadb4c30aaed334a9fc8a0006 | YES | ⛑ THE INSTALL IS REAL, THE STAMP IS NOT — the same split the 2026-08-05T18:40:00Z row records, and for the same reason. `emit_descriptors.py` installs and stamps in one step with no install-without-stamp path, so the run above wrote both; the stamp was restored to its previous bytes and only the descriptor install kept. WHY: `metatheory/Dregg2` was DIRTY (sibling lanes live), so the stamp it minted recorded `source_dirty=true` — a stamp `--verify-provenance` REFUSES outright, which would have replaced a truthful "these by-name artifacts carry no stamp row" signal with a blanket "this stamp is invalid". WHAT INSTALLED: exactly ONE file, `by-name/dregg-mina-lightclient-verify-v1.json`, from `LightClientMinaAir.minaLcVerifyDesc`. WHAT MOVED IN IT: `trace_width` 49 → 58 (nine `LINK_VK` columns) and 62 → 63 constraints (a SECOND `proof_bind`, guard col 8 = `LINK_OK`, commit cols 21..29 = `TIP_STATE`, vk cols 49..57, `vk_pin` = the nine `Faithful9` lanes of `dregg-mina-lightclient-link::v1`'s semantic fingerprint). **`public_input_count` STAYS 30 and no PI slot moves** — the seam's commitment is the tip block the descriptor already published. **THE HEAD VK ROTATES**; every previously produced `MinaHeadProofWire` now fails to verify, which is the intent. `mina_head_predicate_vk()` is blake3 over the descriptor NAME and does NOT move, so cell programs keep their pinned predicate vk and nothing re-genesises. ⚠ NEW COUPLING: re-emitting `dregg-mina-lightclient-link-v1.json` now moves `LightClientMinaAir.LINK_VK_LANES` and therefore re-emits and re-VKs the head, exactly as `pasta-fq-chainlink.json` already does through `CHAINLINK_VK_LANES`. | epoch:unchanged |
| 2026-08-06T00:12:17Z | ember@nextop.local | emit | 159b2b42b5d7476acc22d40d195ce9ab0739ab3f | 405f33ddcf69eb75693f2b2fd25f31a709d30663 | YES | PROVENANCE.json: descriptor_sha256: `dregg-bilateral-aggregation-v3.json` pin does not equal the emitted bytes, PROVENANCE.json: descriptor_sha256: `dregg-bundle-tree-fold-v2.json` pin does not equal the emitted bytes, PROVENANCE.json: descriptor_sha256: `dregg-cert-f-ir2.json` pin does not equal the emitted bytes, PROVENANCE.json: descriptor_sha256: `dregg-cert-f-market4-ir2.json` pin does not equal the emitted bytes, PROVENANCE.json: descriptor_sha256: `dregg-cross-cell-conservation-v2.json` pin does not equal the emitted bytes, PROVENANCE.json: descriptor_sha256: `dregg-cross-side-existence-v2.json` pin does not equal the emitted bytes, … +145 | epoch:23 |
| 2026-08-06T00:20:00Z | ember@nextop.local | record-only (correcting the 2026-08-06T00:12:17Z row ABOVE: it installed NOTHING and its PROVENANCE.json STAMP was deliberately REVERTED; PROVENANCE.json is unchanged from the 2026-08-03T02:32:30Z stamp) | 159b2b42b5d7476acc22d40d195ce9ab0739ab3f | 405f33ddcf69eb75693f2b2fd25f31a709d30663 | YES | ⛑ A NO-OP INSTALL AND A REVERTED STAMP, the same split the 2026-08-05T18:40:00Z and 2026-08-06T00:10:00Z rows record and for the same reason. The 00:12:17 run re-derived all 193 descriptors and found every one byte-identical to the Lean emission, so it wrote no descriptor at all; `emit_descriptors.py` still stamps in the same step, so it wrote PROVENANCE.json, and that stamp has been restored to its previous bytes. WHY: `metatheory/Dregg2` was DIRTY (sibling lanes live), so the stamp recorded source_dirty=true, which `--verify-provenance --strict` REFUSES outright; taking it would replace a truthful "these by-name artifacts carry no stamp row" signal with a blanket "this stamp is invalid". WHAT THE RUN DID ESTABLISH: the two NEW by-name artifacts installed by the 00:07:17 row — `mina-wrap-conjunction.json` and `mina-wrap-conjunction-unthreaded.json`, `MinaWrapConjunctionAir.{conjunctionDesc, conjunctionUnthreadedDesc}` — re-derive BYTE-IDENTICALLY from the Lean emitters, which is the second-run-moves-zero-bytes property those rows need. WHAT THEY ARE: the wrap conjunction with its b-polynomial THREADED — 15 rounds are 15 rows carried by 448 `.transition` legs, `trace_width` 2 536 at every round count against the withheld row-local layout's 22 184, 4 157 constraints against 30 607. The `-unthreaded` twin is the same Lean AIR minus the 448 window legs and exists as the negative control for the falsifier. NOTHING RE-EMITS and NO VK ROTATES: both names are NEW, no existing descriptor's bytes moved, and `byNameDescriptors_length` moves 109 -> 111. | epoch:23 |
| 2026-08-06T06:43:51Z | ember@nextop.local | stamp-reconciliation (NOT a regen: a full re-derivation from Lean moved ZERO descriptor bytes; the driver's own `mode` string for this run is `emit` and the stamp records `"mode": "emit"`, which is the true claim — these hashes come from the emitters, not from a disk re-hash) | 6b6c2a4c05398ceb08bddcb14aff2d40c2db99c3 | c5ab6469e8999e58192470949ef59b46422e0202 | no | ⚑ **ONLY `circuit/descriptors/PROVENANCE.json` CHANGED.** Not one descriptor byte, not one `*_FP` constant, not one `*_generated.rs` module moved — so **NO VK rotates, NOTHING re-emits and NOTHING re-genesises**, and `CANONICAL_STATE_SCHEMA_EPOCH` stays at 23. **HOW THAT WAS ESTABLISHED, twice, from the emit's own output at `c5ab6469e`:** (a) the UN-ACKED run refused (exit 3, tree untouched) with 151 reasons whose FIRST entry is a `PROVENANCE.json:` stamp shortfall — `install_and_stamp` builds `reasons = changed + stamp_gap` with `changed` FIRST and `changed` entries are bare descriptor filenames or repo-relative Rust paths, so a leading `PROVENANCE.json:` proves `changed` was **EMPTY**; (b) the ACKED run then printed `STAMP-ONLY REGEN — all 193 descriptor files were already byte-identical to the Lean emission, but PROVENANCE.json did not attest 151 of them`, which is that function's `if not changed:` branch. **WHAT THE STAMP NOW COVERS:** `by_name_sha256` 76 → 111 rows (35 NEW: the 11 formerly hand-driven `mina-commit-*`/`mina-xi-*`, `mina-wrap-conjunction{,-unthreaded}`, `dark-bazaar-private-poa-settlement-n4k4-v4`, `dregg-mina-lightclient-{verify,link}-v1`, `dregg-solana-stake-table-fold-v1`, `pasta-alu-{,fq-}sound`, `pasta-sbox-prog{,-1k}`, `pasta-fq-chainlink`, the six `pasta-f{p,q}{mul,add,sub}-sound` and the rest of the Pasta round/absorb set) plus 76 MOVED hashes; `descriptor_sha256` 40 MOVED of 71; `table-airs_sha256` 11 UNCHANGED; `fp_file_sha256` 2 MOVED (`circuit/src/effect_vm_descriptors.rs`, `circuit/src/lean_descriptor_air.rs`) — 35 new + 116 moved = exactly the 151 the refusal named. **WHY IT WAS OUTSTANDING:** the 2026-08-05T18:24:25Z and 2026-08-06T00:04:01Z / 00:07:17Z / 00:12:17Z runs INSTALLED those bytes but minted their stamps with `metatheory/Dregg2` DIRTY (`source_dirty=true`, a stamp `--verify-provenance` refuses outright), so each stamp was correctly REVERTED — see the three `record-only` rows above. The bytes have been the Lean emission since; nothing attested them. **HOW THIS ONE WAS TAKEN:** in a DETACHED `git status`-clean worktree at `c5ab6469e` (`git worktree add --detach`, `metatheory/.lake` COW-seeded, then `lake build Dregg2` + the 158 `--list-emitter-modules` targets rebuilt so the emit read FRESH oleans), with NO `DREGG_VK_REGEN_ALLOW_DIRTY` — hence `source_dirty=false`. **MEASURED AFTER:** `--verify-provenance --rev HEAD` goes 186 findings → **0** (118 of those were `does NOT match its stamped sha256`; the other 68 were 35 UNSTAMPED + 33 `on disk but NOT covered`), and `--verify-provenance --strict` PASSES over 198 artifacts. | epoch:23 |
| 2026-08-06T12:35:35Z | ember@nextop.local | emit | fe3706460cde8a8a28f699d416406024b227c944 | ee147e669a8100009ed20fe1355b1073e66a9c30 | YES | by-name/adjacency-membership-wide.json, by-name/automatafl-legc-n11.json, by-name/automatafl-legc-n5.json, by-name/automatafl-resolve-marks-n11.json, by-name/automatafl-resolve-marks-n2.json, by-name/automatafl-resolve-n11.json, … +89 | epoch:23 |
| 2026-08-06T22:30:26Z | ember@nextop.local | emit | e06643027c58885d1d7d9e4f53fdc2617f65436c | e0a391b1d466046c3bd7f1d79dbb3a3bf8aec358 | no | by-name/adjacency-membership-wide.json, by-name/blinded-membership-4ary-wide.json, by-name/committed-threshold.json, by-name/compound-predicate.json, by-name/dyck-parse.json, by-name/effect-burn.json, … +95 | epoch:23 |
| 2026-08-06T22:45:00Z | ember@nextop.local | record-only (NO emit — reconciling the TWO `emit` rows above, whose INSTALLS are the same bytes and whose STAMPS are not) | e06643027c58885d1d7d9e4f53fdc2617f65436c | e0a391b1d466046c3bd7f1d79dbb3a3bf8aec358 | false | ⛑ **THE 12:35:35Z ROW'S INSTALL STANDS; ITS STAMP IS DISCARDED, AND THAT IS A MEASURED CLAIM.** That run emitted from a tree with `metatheory/Dregg2` DIRTY, so it needed `DREGG_VK_REGEN_ALLOW_DIRTY=1` and its stamp recorded `source_dirty=true` at `repo_head` `ee147e669` — a stamp `--verify-provenance` REFUSES outright, i.e. the failure mode this log already records on 2026-08-05. It then sat uncommitted in the shared tree for ~10h and tripped three lanes, one of which attributed 30 test failures to it. Rather than trust or discard it, the 22:30:26Z run RE-DERIVED the whole set independently: a detached worktree at `e0a391b1d`, verified `git status`-clean before anything read it, `.lake` COW-seeded, all twelve emitters to completion. **RESULT: all 34 orphaned artifacts are BYTE-IDENTICAL to the fresh emission, and so are all nine `*_FP`-bearing Rust sources** (`diff -rq` over `circuit/descriptors` clean; the two `V3_STAGED_REGISTRY_FP` / `WIDE_REGISTRY_STAGED_FP` re-pins in `effect_vm_descriptors.rs` reproduce exactly). So the orphan was a COMPLETE and CORRECT emission whose ONLY defect was the stamp. NO descriptor byte differs between the two rows; the 22:30:26Z stamp is `source_dirty=false` against a real reviewable tree, and it is the one in the tree. | epoch:23 |
| 2026-08-07T04:10:00Z | agent:committee-per-turn-value | classify-and-migrate (a SIGNED PREIMAGE moved; NO emit ran, NO descriptor changed, NO VK rotates) | HEAD | (this commit) | false | ⚑ **THE FINALIZATION VOTE NOW COVERS A PER-TURN VALUE — `dregg_types::finalization_vote_signing_message` v3 -> v4.** `"dregg-finalization-vote-v3" ‖ block_id ‖ merkle_root` becomes `"dregg-finalization-vote-v4" ‖ block_id ‖ merkle_root ‖ 0x00 \| 0x01‖<32>`, the third component being the finalized block's `receipt_stream_root` (`0x00`/`0x01‖32` framed, the same discipline `AttestedRoot::signing_message` uses, so an absent stream cannot alias a present one). **WHY:** no `>= threshold` committee signature anywhere in this tree covered a value derived from the TURN. The only preimage that absorbed `receipt_stream_root` — `AttestedRoot::signing_message()` — gets exactly ONE push, the local node's (`blocklace_sync.rs`), because `PeerMessage::AttestedRootUpdate` has zero handlers; the quorum that DOES assemble signed a block id and a whole-ledger BLAKE3 image. `dregg_federation::TurnAnchorV1::verify` therefore REFUSED on every federation with `threshold > 1`. **WHAT MOVED WITH IT, in this commit:** the vote struct gains `receipt_stream_root` (postcard wire, non-self-describing — a mixed-version mesh fails to DECODE, not merely to agree); `VoteCollector` agreement is now over the PAIR `(merkle_root, receipt_stream_root)`; the verified Lean twin's wire moved to `VOTE := signer:ledgerRoot:streamRoot` with `Root := Nat × Nat` and the new named theorems `FinalizationQuorum.quorum_binds_snd` / `FinalityGate.quorum_gate_binds_receipt_stream` (a v3 wire is `"ERR"`, fail-closed); `verifier/src/cross_fed.rs`'s hybrid check moves off `signing_message()` onto the new `AttestedRoot::hybrid_quorum_message()` — it was checking finalization-vote signatures against the attested-root preimage, so **a live root's hybrid quorum could not pass**, invisible because every test in that module signed `signing_message()` by hand. **WHAT REFUSES TO LOAD / RE-GENESISES:** every persisted `StoredAttestedRoot::finalization_quorum` signed under v3 — `verify_finalization_quorum` REFUSES it rather than reinterpreting, so an epoch-23 store's committee restart anchor fails closed; `enforce_canonical_state_schema_epoch` refuses a store stamped 23. Re-genesis the devnet. **⚑ THE PUBLISHED LEAN SEED MUST BE REPUBLISHED** (`dregg-lean-ffi/scripts/seed-dregg2-closure.sh`, then `fetch-lean-seed.sh` consumers): a seed exporting the v3 `dregg_finalization_quorum` returns `"ERR"` on every v4 wire, so `verified_quorum_reached` fails CLOSED and NO block reaches consensus attestation. MEASURED, not assumed: a local build DOES pick the new gate up — `dregg-lean-ffi`'s build script layers the freshly built `.lake` objects over the read-only seed, and `verified_finalization_quorum_matches_guards` passes under `DREGG_TEST_REQUIRE_LEAN=1` (which PANICS on an absent export) asserting `Some((7,9))` on the v4 wire and `None` on the v3 one. A fresh checkout that only fetches the published seed is the case that needs the republish. **WHAT DOES NOT MOVE:** no VK rotates, no descriptor re-emits, no PI count or chip arity changes. `receipt_stream_root` was already a field of `AttestedRoot`/`StoredAttestedRoot` and already in the attested-root preimage (v4/#80); what changed is WHICH SIGNATURES cover it. | epoch:24 |
| 2026-08-07T06:50:00Z | agent:shield-executor-route | classify-and-migrate (a WIRE SHAPE and a VERIFICATION INPUT moved; NO emit ran, NO descriptor changed, NO VK rotates) | HEAD | (this commit) | false | ⚑ **THE SHIELDED SPEND IS JUDGED AGAINST THE EXECUTOR'S OWN ACCUMULATOR — seam #15, closed on the deployed path.** SUBSTRATE: the AIR is AUTHORED IN LEAN — the spend relation is the Lean-emitted `dregg-shielded-spend-complete-fsi2::v1` (`metatheory/Dregg2/Circuit/Emit/ShieldedSpendCompleteEmit.lean`, 557 columns, 25 PIs `[nullifier] ++ committedRoot[8] ++ wide[16]`), read byte-pinned out of the Lean source; Rust authors no constraint. **THE WOUND:** `ShieldedTransferPayload` carried `merkle_root`, chosen by the prover and compared against nothing — `ShieldedMerkleRootPin.root_substitution_forges`. **THE FIX:** the field is DELETED and `apply_shielded_transfer` GATE 0 reads `note_shielded.root8()` from live state, passing it to `ShieldedTransferVerifier::verify` as `piCommitted`; a forged tree folds to `R != root8()` and the 8-lane `rootPins` `.piBinding` has no satisfying assignment. **EVIDENCE:** `executor_theft_forged_tree_refuses_on_the_deployed_path` submits ONE payload to TWO executors differing only in committed state — ACCEPTED by one holding the attacker's tree (so the payload is well formed), REFUSED by one holding the real accumulator, spending and appending nothing. 11/11 `shielded_executor` tests green, 0 ignored (they had been `#[ignore]`d because the deployed path admitted nothing at all: the retired spend circuit exposed only a one-felt `value_binding`, so the same-opening wide join was passed the EMPTY ring vector and failed closed; the complete spend PI-pins its own sixteen `cap_node8` lanes, so the join now compares two independently proven openings). **WHAT REFUSES TO LOAD / RE-GENESISES:** postcard is non-self-describing, so a pre-cutover `ShieldedTransferPayload` REFUSES TO DECODE rather than being reinterpreted (`merkle_root` removed; `ShieldedInputPayload` gains `spend_wide_binding: [u32;16]`; `spend_proof` becomes an `Ir2BatchProof` against the complete-spend relation instead of a `DslZkProof` against the 3-PI `dregg-shielded-spend-v1`). Every shielded-transfer `effects_hash` MOVES (the root was absorbed first), so every receipt, executor signature, receipt QC and attestation over such a turn is invalid. Transcript domains bumped: `dregg-shielded-transfer-v2-committed-root`, `dregg-shielded-transfer-wide-binding-v2`, `dregg-shielded-pool-v2-committed-root`. `WideValueBindingError::RingWideBindingCountMismatch` DELETED (it WAS the deployed fail-closed refusal and is now unreachable). `enforce_canonical_state_schema_epoch` refuses a store stamped 24 — re-genesis the devnet. **WHAT DOES NOT MOVE:** no VK rotates, no descriptor re-emits, no PI count or chip arity changes, and no rotation-carrier limb is added — so the signed consensus anchor is BYTE-IDENTICAL. Committing `note_shielded.root8()` into that carrier needs a fifth `V9RotationContext` root (a new base limb plus a completion octet in the Lean-emitted `EffectVmEmitRotationV3`) and is a SEPARATE VK epoch, deliberately not folded in here. **NAMED RESIDUAL:** `apply_shielded_transfer` GATE 4 still appends the output leg's Ristretto bytes, so `ShieldedOnRampPin.l4_pin_over_deployed_append_relocates_15` is not discharged by a theorem; it is blocked in deployment by an ENCODING DISJOINTNESS (a spendable leaf address is `felt_to_bytes32(cCM)` — 4 significant bytes, 28 zero — and a valid compressed Ristretto point in that subspace is ~2^-224), checked only by the Rust KAT `transfer_output_append_is_prover_written_and_cannot_be_opened_by_any_spend`. `ShieldedMerkleRootPin` §6.5 records why the Lean falsifier is therefore NOT flipped. | epoch:25 |
| 2026-08-07T12:00:00Z | agent:value-link | classify-and-migrate (a WIRE SHAPE moved and a NEW Lean-authored AIR landed; the new descriptor needs an emit, NO existing descriptor changed) | HEAD | (this commit) | YES | ⛑ **THE SHIELDED TRANSFER'S VALUE LINK IS NOW A CIRCUIT EQUALITY — the residual seam #15 named, closed.** SUBSTRATE: the AIR is AUTHORED IN LEAN — `dregg-shielded-transfer-value-link::v1` (`metatheory/Dregg2/Circuit/Emit/ShieldedTransferValueLinkEmit.lean`, 164 columns, 17 PIs `wide[16] ++ [outCm]`, byte-pinned by `link_emits_golden` = `native_decide` + `#assert_compiled`); Rust parses the golden and authors no constraint. **THE WOUND, quoted from the lane that left it:** *"the Pedersen leg's own `v` is tied to the STARK-side `v` by the TRANSCRIPT, not by any circuit equality (`verify_value_link` is test-only)"* — so a spender who genuinely owned a note worth 1 published input/output Ristretto legs committing to 1_000_000, `verify_full_conservation_bytes` cleared `Σ C_in = Σ C_out` over those PROVER-CHOSEN legs, and nothing compared them to the note. **WHY NO CHECK CLOSES IT:** a BabyBear STARK cannot open a Ristretto point without non-native curve arithmetic in-AIR, and a Ristretto sigma protocol cannot open a Poseidon2 image without the hash in-group — `cell-crypto/src/value_link_zk.rs` records the same finding and names the exit (*"faithfully encode value/asset in a wide in-AIR hash commitment and enforce no-mint in AIR"*). **THE FIX:** the Pedersen half is DELETED and one Lean relation reads ONE set of canonical 16-bit limb columns for BOTH the spent note's sixteen `cap_node8` carrier lanes (absorb block IMPORTED from `WideValueBindingEmit`, so it is the same term the complete spend PI-pins) AND the minted note's commitment `hash_fact(vmod,[amod,outOwner,outRand])`. Range proofs go with the group that needed them: the limb cells' booleanity is FORCED, so `0 <= v < 2^64` is a property of the trace. **EVIDENCE (both poles, constructive):** `value_link_divergence_refuses_on_the_deployed_path` proves a GENUINE link at v'=1_000_000 and staples it to a GENUINE spend of a note worth 1 — the divergence is asserted present (carrier differs, minted commitment differs) and the forged link is shown to verify against ITS OWN public inputs, so nothing is malformed — then the executor REFUSES it, spending and appending nothing, while the honest transfer of the same note is admitted. `honest_transfer_conserves_and_the_minted_note_is_spendable` recomputes the minted leaf from an independently built witness worth the same `u64` and then SPENDS it. Lean side: `value_link_reads_one_opening` + `decoupled_outcm_unsat` + `decoupled_carrier_unsat`, `#assert_axioms`-clean, no `#guard` in the file. **WHAT RE-EMITS / REFUSES TO LOAD:** `dregg-shielded-transfer-value-link::v1` is a NEW descriptor (emit + VK epoch for the shielded family); a pre-cutover `ShieldedTransferPayload` REFUSES to decode; every shielded-transfer `effects_hash` moves; `CANONICAL_STATE_SCHEMA_EPOCH` 25 -> 26 and a store stamped 25 is refused, not migrated. **WHAT ALSO CHANGED, and it is a safety change:** `note_shielded` used to hold TWO shapes — Shield-minted Poseidon2 commitments (spendable) and transfer-minted Ristretto points (openable by no circuit, so value entering one was unrecoverable). It holds ONE now, and every appended leaf is spendable. **WHAT DOES NOT MOVE:** no rotation-carrier limb, so the signed consensus anchor is byte-identical. **NAMED RESIDUALS:** deployed arity is 1-in/1-out and every other arity REFUSES (1-in/2-out with change is the next descriptor in the Lean family — the limbwise carry chain); note-plaintext delivery to the recipient is out of band; and the wide sidecar relation is no longer on the transfer path. | epoch:26 |
| 2026-08-07T17:44:36Z | ember@nextop.local | emit (a PI LAYOUT moved; every EffectVM v1/rotated/wide descriptor re-emitted, VK rotates, NO re-genesis) | da5eb3503291d115cbd2e4d390d69f150cdf13ed | 9bdab9b5b5e5bccb8cc2963b6bff920b70208ee2 | no | **THE SEVEN-SLOT PI COMPACTION (`docs/PI-DISPOSITION.md` §6, source commit `9bdab9b5b`).** 61 changed descriptor files + `rotation-v3-staged-registry.tsv` + `rotation-wide-registry-staged.tsv` + `rotation-layout-v3-staged.json` + `PROVENANCE.json`, and the FP re-pins in `circuit/src/effect_vm_descriptors.rs` (`V3_STAGED_REGISTRY_FP`, `WIDE_REGISTRY_STAGED_FP`), `circuit/src/cap_delegation_nonamp_descriptor.rs` and `circuit/src/effect_vm/umem_weld_generated.rs`. `dregg_epoch::local_manifest`'s `registry_fp` is DERIVED from those two constants, so it moves with them and a peer on the old `descriptor_set_tag` refuses the handshake — the correct loud break. **WHAT MOVED:** v1 PI offsets 26..32 (`CURRENT_BLOCK_HEIGHT`, `MAX_CUSTOM_EFFECTS`, `CUSTOM_EFFECT_COUNT`, `APPROVED_HANDOFFS[4]`) are DELETED, so `V1_PI_COUNT` 42 → 35 and `ROT_PI_COUNT` 46 → 39. **EVERY member of both rotation registries moved by exactly −7** and nothing else changed: Σ `piCount` 3,821 → 3,429 wide (57 members) and 3,012 → 2,599 v3 (60 members), which reproduces `docs/PI-DISPOSITION.md` §6's priced figures to the felt. `transferVmDescriptor2R24` 68 → 61 wide / 50 → 43 v3; `burnVmDescriptor2R24` 66 → 59. **WHAT REFUSES RATHER THAN REINTERPRETS:** a pre-compaction proof carries 46 PIs where the descriptor now declares 39, so Ir2 verification fails on the PI-count mismatch BEFORE any constraint is evaluated — an old proof cannot be re-read under the new layout. **WHAT DOES NOT MOVE:** `CANONICAL_STATE_SCHEMA_EPOCH` stays 26 — no `state_commit` input changed, so the compaction rides past every persisted store and NO re-genesis is required. This is a VK rotation for the EffectVM family and nothing more. ⓵ Emitted from a DETACHED, `git status`-clean worktree at `9bdab9b5b` with a COW-seeded `.lake` (3,466 lake jobs replayed, zero rebuilds), so the stamp is `source_dirty=false` rather than the ALLOW_DIRTY stamp `--verify-provenance` refuses — the shared tree was dirty from four other lanes and could not have produced a reviewable one. | epoch:26 |
| 2026-08-08T15:56:57Z | ember@nextop.local | emit | fc36697403e402e8ac7b793f78839cb3165fa0ff | c948081df944eb15c4658a298a760deb26f5ad10 | YES | by-name/dregg-mina-body-preimage-bits-v1.json, circuit/src/effect_vm/layout_generated.rs, PROVENANCE.json: by_name_sha256: `dregg-mina-body-preimage-bits-v1.json` was emitted but has NO row in the stamp, PROVENANCE.json: by_name_sha256: `dregg-mina-lightclient-link-v1.json` pin does not equal the emitted bytes, PROVENANCE.json: by_name_sha256: `dregg-mina-lightclient-verify-v1.json` pin does not equal the emitted bytes | epoch:26 |
| 2026-08-08T16:02:18Z | ember@nextop.local | emit | 493afd3d42a588a305a7304bdc61c82437d50bb4 | 4c354750f507900271294aa6a2ff922bd3fd81e6 | YES | rotation-wide-registry-staged.tsv, circuit/src/effect_vm_descriptors.rs, circuit/src/effect_vm/layout_generated.rs, PROVENANCE.json: descriptor_sha256: `rotation-wide-registry-staged.tsv` pin does not equal the emitted bytes | epoch:26 |
| 2026-08-08T16:30:00Z | agent:key-nonet-ninth-lane | record-only (annotating the TWO auto rows above; the 16:02:18Z INSTALL stands and IS the flag day, the 15:56:57Z one emitted from STALE oleans and installed only `layout_generated.rs` with a matrix that has since been overwritten) | 493afd3d42a588a305a7304bdc61c82437d50bb4 | (this commit) | YES | ⛑ **THE SOVEREIGN KEY_COMMIT TEETH NOW READ THE NINTH NONET LANE — a key and its NEGATION are separable at the deployed prover.** SUBSTRATE: Lean-authored AIR. `Emit.CarrierOctetGates.quadIdx` row 2 moved `[0,4,2,6]` -> `[8,0,4,2]`; `keyCommitSpec`/`quadCols` read a NONET column map (lanes 0..7 at `B_PUBKEY8`, lane 8 at `B_PUBKEY_NINTH_LANE`) instead of the eight-column octet. Rust authors no constraint. **WHAT RE-EMITS AND WHAT ROTATES:** exactly ONE descriptor file, `circuit/descriptors/rotation-wide-registry-staged.tsv` (`WIDE_REGISTRY_STAGED_FP` re-pinned in `circuit/src/effect_vm_descriptors.rs`), plus `circuit/src/effect_vm/layout_generated.rs` gaining the emitted `KEY_COMMIT_QUAD_IDX` / `KEY_NONET_LANES`. On the deployed `makeSovereignVmDescriptor2R24` row exactly ONE constraint changed: key-commit chip lookup #3's inputs `(203,207,205,209)` -> `(284,203,207,205)`. **NOTHING ELSE MOVED** — trace_width 1831, public_input_count 71, 616 constraints, `KEY_COMMIT_SPAN` 32, chip arity 4, all UNCHANGED. So the VK rotates for the wide registry and **NO re-genesis is required**: no committed state encoding moved and `CANONICAL_STATE_SCHEMA_EPOCH` stays 26. **WHAT NOW REFUSES TO LOAD:** a wide proof minted against the pre-flag-day `makeSovereign` VK. **THE WOUND:** `6441705e8` rewrote `dregg_commit::typed::canonical_32_to_felts_4` into one arity-16 nonet absorb on a census that called the four-felt id fold off-AIR; it missed `TurnExecutor::pubkey_to_witness_key_commit`, which IS that function and IS on-AIR, so the executor and the AIR checked different functions and `rotated_sovereign_make_sovereign_proves_and_verifies` went red on `InvalidPowWitness` while its nine record-pin siblings passed. The cheap fix (re-point the executor at the octet recipe) was REFUSED: it re-opens the A/-A blindness, since RFC 8032 puts an Ed25519 x-sign in source bit 255 = nonet lane 8 and NO other lane. **EVIDENCE:** `circuit-prove/tests/sovereign_key_negation_teeth_separate.rs` exhibits a real curve25519 pair `(A, -A)` publishing different teeth (exactly the one quad that reads lane 8 moves), keeps the RETIRED eight-lane recipe in-file as the refutation object and shows it MERGED that pair, and drives both poles at the deployed prover: the honest owner accepted, a leg whose committed nonet is `-A` carrying `A`'s teeth REFUSED by a violated CONSTRAINT (`assert_violated_constraint_not_bus`). `sdk/tests/sovereign_rotated_c1.rs` 26/26. **ONE DENOTATION:** `effect_vm::helpers::key_commit_teeth_from_nonet`, called by the executor AND by `trace_rotated::append_sovereign_key_commit_rider`, over a `KEY_COMMIT_QUAD_IDX` GENERATED from the Lean `quadIdx` by `EmitLayoutManifest`. | epoch:26 |
| 2026-08-10T23:00:49Z | ember@nextop.local | emit | c3eeb1fda0c8859d0c4da6897299b92cdbcb8110 | 2ad7e48c8f645cb3227b917a067a73a45c9d6a5c | YES | by-name/dregg-mina-body-preimage-bits-v1.json, by-name/dregg-mina-lightclient-link-v1.json, by-name/dregg-mina-lightclient-verify-v1.json, by-name/dregg-mina-preamble-legs-v1.json, by-name/mina-accumulator-head-genesis.json, by-name/mina-accumulator-head.json, … +21 | epoch:27 |
| 2026-08-11T21:51:29Z | hbox@hbox | emit | 130748d09d4f43d9cefd32a7a0ba54b06393edbd | 544068c3073dd65164a3cce3dfdbf05dc8ead3e4 | no | PROVENANCE.json: by_name_sha256: `dregg-mina-bodyhash-relimb-v1.json` was emitted but has NO row in the stamp, PROVENANCE.json: by_name_sha256: `dregg-mina-lightclient-link-v1.json` pin does not equal the emitted bytes, PROVENANCE.json: by_name_sha256: `dregg-mina-lightclient-verify-v1.json` pin does not equal the emitted bytes, PROVENANCE.json: by_name_sha256: `mina-accumulator-head-genesis.json` pin does not equal the emitted bytes, PROVENANCE.json: by_name_sha256: `mina-accumulator-head.json` pin does not equal the emitted bytes, PROVENANCE.json: by_name_sha256: `pasta-fp-absorb.json` pin does not equal the emitted bytes, … +9 | epoch:27 |
