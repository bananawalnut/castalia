.claude
.claire

hints/benches/*.bincode
hints/benches/committee_1024.json
hints/benches/committee.json
hints/benches/committee_128.json

extension/dist
site/dist
site/assets/dregg.pdf
site/paper/
paper/*.pdf

target
node_modules
__pycache__
.playwright-mcp

# Devnet secrets
docker/devnet-config/*
!docker/devnet-config/.gitkeep
!docker/devnet-config/README.md
deploy/genesis/keys/
deploy/genesis/*.key
deploy/genesis/secrets/
demo/two-ai-handoff/state/
demo/multi-node-devnet/state/

CLAUDIT.md

states/
spec/states

*.zip
*.xpi

metatheory/.lake
lean-objs

libdregg_lean.a*
# Starbridge Lean-as-web-citizen workspace: 3.7GB lean4-src checkout + WASM
# build spikes; a build/scratch area, never committed.
/web
/target-swaplane
/site/pkg
/sdk-ts/dist
sel4/dregg-pd/executor-microkit-pd/microkit-patch/microkit-2.2.0-patched

# build artifacts — never checkpoint compiled output into history
*.wasm
*.rlib
*.rmeta
*.dylib

# macOS finder junk
.DS_Store
discord-values

# night-shift render frames — artifacts, not source (ember: do not check PNGs into git)
night-shift-woven.png
*.woven.png
deos-*-woven*.png

# session working notes + product planning — NOT substrate (kept private, out of the public repo)
DEOS-NIGHT-SHIFT.md
PLANS-BREADCRUMBS.md
DREGG-COMPUTER.md
DREGG*-MIGRATION.md

# regenerable artifacts (stitch)
# These seven Lean-emitted TSVs are hydrated from the private, hash-pinned
# descriptor store because they are include_str!'d by dregg-circuit. Keep the
# allowlist explicit so unrelated descriptor artifacts remain visible to Git.
circuit/descriptors/rotation-v3-setfield-value8-staged-registry.tsv
circuit/descriptors/rotation-v3-staged-registry.tsv
circuit/descriptors/rotation-wide-registry-staged.tsv
circuit/descriptors/rotation-wide-transfer-staged.tsv
circuit/descriptors/rotation-wide-umem-welded-registry-staged.tsv
circuit/descriptors/umem-cohort-multidomain-v1-staged-registry.tsv
circuit/descriptors/umem-cohort-v1-staged-registry.tsv
demo/run/*.png
orb/conformance/h2c-host/h2c-host
*.png.skip
wasm/pkg-mwt/
orb/conformance/h2c-host/h2c-host
