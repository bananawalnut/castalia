[workspace]
resolver = "3"
# wasm is EXCLUDED from the workspace: its `no-lean-link` platform-gate
# enables would otherwise feature-unify onto dregg-turn/sdk/etc in any
# whole-workspace build (cargo test --workspace), deleting the Lean paths
# on native. It builds standalone via wasm-pack (scripts/build-web-artifacts.sh,
# pages.yml), which uses its manifest path directly.
# members += the 12 absorbed deos/starbridge elephant crates (formerly standalone
# workspaces) so they share ONE target dir + one resolve (the disk + CI win).
# `discord-bot` is NOT a member: it (+ deos-zed-full) pull sqlx/sqlez →
# libsqlite3-sys 0.30, whose `links = "sqlite3"` conflicts with deos-matrix's
# rusqlite-0.35 (matrix-sdk-sqlite). Cargo allows ONE `links="sqlite3"` per
# workspace, so those two stay separate workspaces (discord-bot excluded below,
# deos-zed-full still excluded). `default-members` (below) is the light protocol/
# circuit/app set so bare `cargo build`/`cargo test` stays gpui/mozjs/servo-free.
#
# ⚠ TWO MEMBERS CANNOT LINK ON LINUX, AND A `--workspace` BUILD OR TEST MUST NAME THEM.
# `grain-verify-wasm` and `starbridge-v2/web` (package `starbridge-web`) are wasm-bindgen
# bundles that declare `crate-type = ["cdylib", "rlib"]`, and both reach `dregg-lean-ffi`
# unconditionally on native (grain-verify's Lean-proven R3 verify core; starbridge-v2's
# `embedded-executor` → dregg-sdk). `cargo build`/`cargo test` builds the cdylib for the
# HOST target too — an artifact nothing ever loads — and on ELF that link fails:
#     rust-lld: error: relocation R_X86_64_TPOFF32 against _mi_heap_default
#               cannot be used with -shared
# `libleanrt.a:static.c.o` — the Lean 4.30.0 toolchain's bundled mimalloc — uses local-exec
# TLS, which is only legal in a main executable. Measured 2026-07-26 on persvati: both
# crates fail this way; the same `cargo build -p grain-verify-wasm` LINKS on macOS (Mach-O
# has no equivalent rule), which is why the macOS `cargo test --workspace` job is green and
# the Linux one has been unable to complete.
# The mimalloc objects ride INSIDE `dregg-lean-ffi`'s rlib — rustc bundles `static=`-linked
# native libs into the rlib — and are pulled in to satisfy the `+whole-archive` FFI shim.
# `-Wl,--gc-sections` is already on that link line and cannot help: a rejected relocation is
# a resolution-time error, before any section could be dropped.
# There is NO per-crate opt-out inside one workspace resolve: `dregg-lean-ffi`'s
# `no-lean-link` feature UNIFIES onto every native crate (the reason `wasm` is excluded,
# above) and `DREGG_LEAN_LINK=shared` — the toolchain knob that swaps the static runtime for
# `libleanshared.so` — is an env var, i.e. whole-build, and propagates no rpath downstream.
# So a Linux workspace test is TWO invocations, and the second is not optional:
#     cargo test --workspace --exclude deos-zed \
#         --exclude grain-verify-wasm --exclude starbridge-web
#     cargo test -p grain-verify-wasm -p starbridge-web --lib
# THE SECOND LINE IS WHY THE FIRST COSTS NO TEST. `--lib` selects only the lib-as-test
# target, which rustc builds with `--test` into an EXECUTABLE — never a cdylib — so all 9
# native tests (grain-verify-wasm's 8 agreement tests + starbridge-web's
# `wire_msg_roundtrips`) still run on Linux. Dropping the second line silently narrows the
# broadest gate in the repo by nine; keep them together. What the pair still does not link
# on Linux: the two libs' doctests (measured: zero doc examples in either crate),
# `gen-fixture` (zero #[test]), and starbridge-web's `pty_ws_e2e` / `web_panes_wasm` (both
# already `never-run` rows in `.github/dark-targets.txt`). All four stay covered by
# `cargo check --workspace --all-targets` (which does not link) and by the whole macOS
# `cargo test --workspace`.
# ⚠ `cargo build --workspace` (repro-gate.yml's manual `repro-gate-full-workspace`) hits the
# SAME wall on Linux for the same reason — it is a LINK constraint, not a `cargo test` one.
members = ["macaroon", "secrets", "token", "tokenizer", "hints", "commit", "trace", "demo", "demo-agent", "demo/sdk-consensus", "demo/dungeon-service", "demo/real-dungeon-service", "federation", "dregg-codec", "circuit", "circuit-prove", "recursion-verify", "deco-prove", "audit", "bridge", "wire", "persist", "cell", "cell-crypto", "turn", "turn-prover", "exec-lean", "coord", "tests", "types", "sdk", "dregg-sdk-net", "node", "intent", "net", "discharge-gateway", "teasting", "app-framework", "dregg-payable", "dregg-dsl", "dregg-dsl-runtime", "dregg-dsl-tests", "dregg-dsl-differential", "blocklace", "captp", "cli", "verifier", "protocol-tests", "observability", "storage", "starbridge-apps/nameservice", "dfa", "dfa-federation", "rbg", "credentials", "directory", "starbridge-apps/identity", "starbridge-apps/subscription", "starbridge-apps/governed-namespace", "starbridge-apps/compartment-workflow-mandate", "starbridge-apps/storage-gateway-mandate", "starbridge-apps/privacy-voting", "starbridge-apps/bounty-board", "starbridge-apps/agent-provenance", "starbridge-apps/tool-access-delegation", "starbridge-apps/sealed-auction", "starbridge-apps/polis", "starbridge-apps/escrow-market", "starbridge-apps/first-room", "starbridge-apps/compute-exchange", "starbridge-apps/gallery", "starbridge-apps/swarm-orchestration", "starbridge-apps/supply-chain-provenance", "starbridge-apps/agent-orchestration", "starbridge-apps/tussle", "starbridge-apps/kvstore", "starbridge-apps/branch-stitch-multiplayer", "starbridge-apps/execution-lease", "starbridge-apps/org", "starbridge-apps/vat", "starbridge-apps/billing", "dregg-sandbox", "starbridge-apps/guard", "starbridge-apps/domains", "starbridge-apps/edge-mandate", "dregg-umem", "dregg-storage-templates", "preflight", "dregg-lean-ffi", "lightclient", "redteam", "perf", "dregg-auth", "dregg-query", "dregg-merge", "dregg-userspace-verify", "dregg-deploy", "dregg-analyzer", "dregg-mirror", "dregg-epoch", "dregg-genesis-snapshot", "deos-reflect", "starbridge-v2", "starbridge-v2/web", "deos-js-core", "deos-js", "deos-js-runtime", "deos-view", "deos-zed", "deos-matrix", "deos-hermes", "deos-terminal", "servo-render", "deos-web-cells", "starbridge-web-surface", "deos-leptos", "android-cell", "mobile/deos-core-smoke", "dregg-agent", "grain-turn", "hosted-lease", "grain-verify", "grain-verify-wasm", "grain-fork", "sandstorm-bridge", "sandstorm-serve", "hosted-durable", "grain-commons", "http-serve", "agent-platform", "webauth-core", "agent-host", "dregg-ipfs", "grain-jail", "zkoracle-prove", "zkoracle-live", "node-target", "commons-arbiter", "confined-swarm", "auditable-fund", "spween-dregg", "dregg-schema", "dungeon-on-dregg", "collective-choice", "mud-dregg", "attested-dm", "dregg-governance", "interactive-fiction-demo", "dregg-pq", "starbridge-apps/castalia-membership", "dregg-pq-testkit", "narrator", "dice", "pqvrf", "shared-draft", "game-turn-slice", "dregg-automatafl", "param-compose", "entity-compose", "braid-hook", "procgen-dregg", "ugc-dregg", "dregg-pay", "tee-verify", "tee-produce", "attest-check", "dregg-chutes-e2ee", "dreggnet-offerings", "dreggnet-market", "dreggnet-compute", "dreggnet-telegram", "dreggnet-wechat", "dreggnet-web", "dreggnet-discord-identity", "dreggnet-hermes", "dreggnet-grain", "dreggnet-council", "dreggnet-doc", "dreggnet-names", "dreggnet-tournament", "dreggnet-asset", "dreggnet-sprite", "dreggnet-party", "dreggnet-trade", "dregg-season", "dreggnet-cheevo", "dregg-multiway-tug", "dreggnet-craft", "dreggnet-tavern", "dreggnet-faction", "dreggnet-quest", "dreggnet-guild", "dreggnet-saga", "dreggnet-companion", "dreggnet-gear", "dreggnet-adventure", "dreggnet-verified-completion", "dreggnet-surfaces", "dreggnet-catalog", "dreggnet-game-board", "dreggnet-prove-service", "dreggnet-audit", "host-gateway", "starbridge-apps/site-host", "chain", "eth-lightclient", "cosmos-lightclient", "sel4/dregg-firmament", "dregg-doc", "tools/deployer-gate", "crypto-hermine", "crypto-tanuki", "crypto-traccoon", "crypto-hashrand", "crypto-xmvrf", "constraint-lowering", "fhegg-solver", "git-deploy", "deploy/gateway-ask", "fhir", "dregg-oracle", "dregg-interchain-gov", "fhegg-core", "fhegg-fhe", "dregg-stream-ingest", "realm-model", "dregg-drive", "sumcheck-toy", "fold-opening"]


# default-members = the LIGHT protocol/circuit/app crates (members minus the 12
# gpui/mozjs/servo elephants). A bare `cargo build`/`cargo test` (no `--workspace`,
# no `-p <elephant>`) operates on THESE only, so the default dev loop never pulls
# gpui/mozjs/servo. Build an elephant explicitly with `-p starbridge-v2` etc.
default-members = ["macaroon", "secrets", "token", "tokenizer", "hints", "commit", "trace", "demo", "demo-agent", "demo/sdk-consensus", "demo/dungeon-service", "demo/real-dungeon-service", "federation", "dregg-codec", "circuit", "circuit-prove", "recursion-verify", "deco-prove", "audit", "bridge", "wire", "persist", "cell", "cell-crypto", "turn", "turn-prover", "exec-lean", "coord", "tests", "types", "sdk", "dregg-sdk-net", "node", "intent", "net", "discharge-gateway", "teasting", "app-framework", "dregg-payable", "dregg-dsl", "dregg-dsl-runtime", "dregg-dsl-tests", "dregg-dsl-differential", "blocklace", "captp", "cli", "verifier", "protocol-tests", "observability", "storage", "starbridge-apps/nameservice", "dfa", "dfa-federation", "rbg", "credentials", "directory", "starbridge-apps/identity", "starbridge-apps/subscription", "starbridge-apps/governed-namespace", "starbridge-apps/compartment-workflow-mandate", "starbridge-apps/storage-gateway-mandate", "starbridge-apps/privacy-voting", "starbridge-apps/bounty-board", "starbridge-apps/agent-provenance", "starbridge-apps/tool-access-delegation", "starbridge-apps/sealed-auction", "starbridge-apps/polis", "starbridge-apps/escrow-market", "starbridge-apps/first-room", "starbridge-apps/compute-exchange", "starbridge-apps/gallery", "starbridge-apps/swarm-orchestration", "starbridge-apps/supply-chain-provenance", "starbridge-apps/agent-orchestration", "starbridge-apps/tussle", "starbridge-apps/kvstore", "starbridge-apps/branch-stitch-multiplayer", "starbridge-apps/execution-lease", "starbridge-apps/org", "starbridge-apps/vat", "starbridge-apps/billing", "dregg-sandbox", "starbridge-apps/guard", "starbridge-apps/domains", "starbridge-apps/edge-mandate", "dregg-umem", "dregg-storage-templates", "preflight", "dregg-lean-ffi", "lightclient", "redteam", "perf", "dregg-auth", "dregg-query", "dregg-merge", "dregg-userspace-verify", "dregg-deploy", "dregg-analyzer", "dregg-mirror", "dregg-epoch", "dregg-genesis-snapshot", "deos-reflect", "dregg-agent", "hosted-lease", "grain-verify", "sandstorm-bridge", "sandstorm-serve", "hosted-durable", "grain-commons", "http-serve", "agent-platform", "webauth-core", "agent-host", "dregg-ipfs", "grain-jail", "zkoracle-prove", "node-target", "shared-draft", "game-turn-slice", "dregg-automatafl", "param-compose", "entity-compose", "braid-hook", "procgen-dregg", "ugc-dregg", "dregg-pay", "dregg-chutes-e2ee", "dreggnet-asset", "dreggnet-sprite", "dreggnet-party", "dreggnet-trade", "dregg-season", "dreggnet-cheevo", "dregg-multiway-tug", "dreggnet-craft", "dreggnet-quest", "dreggnet-companion", "dreggnet-saga", "dreggnet-adventure", "dreggnet-surfaces", "dreggnet-game-board", "dreggnet-prove-service", "sel4/dregg-firmament", "dregg-doc", "crypto-hermine", "constraint-lowering", "git-deploy", "deploy/gateway-ask", "dregg-stream-ingest", "realm-model"]

# rbg/ (the `dregg-rbg` package) hosts the Robigalia-inspired userspace
# primitives — `DirectoryCell`, `ScopedIntentPool`, `MetaDirectory`,
# `DirectoryFactory`, and the userspace `Volume`/`Blob`/`Directory`
# VFS triple — composed atop `dregg-types::CellId` /
# `dregg-types::FederationId` and (for the directory factory
# descriptor) `dregg-cell::factory::FactoryDescriptor`. The DFA routing
# module that used to live in rbg has been promoted into `dregg-dfa`
# per `DFA-RATIONALIZATION-DESIGN.md`; rbg/ no longer ships its own
# routing engine.
# `discord-bot` + `deos-zed-full` are EXCLUDED (separate workspaces): both pull
# sqlx/sqlez → libsqlite3-sys 0.30, whose `links = "sqlite3"` conflicts with
# deos-matrix's rusqlite-0.35 (one `links="sqlite3"` per workspace). discord-bot
# carries its own `[workspace]`/`[workspace.dependencies]`/`[patch]` + a
# `rust-toolchain.toml` so it still builds standalone.
# `dregg-tui` is EXCLUDED (its own workspace) for a VERSION-PIN reason: it is the
# only `ratatui` user, and ratatui 0.29 pins `unicode-width =0.2.0` EXACTLY, which
# is incompatible with `merman` (pulled by the full-Zed editor surface via
# `starbridge-v2` → `deos-zed-full` → editor → markdown), which needs
# `unicode-width ^0.2.2`. Keeping `dregg-tui` in the root workspace forces both
# constraints into one resolution graph and breaks it. Excluded, it carries its
# own `[workspace]` + `[patch]` (ark-serialize fork) + `rust-toolchain.toml` so it
# still builds standalone (`cd dregg-tui && cargo build`); its path-deps
# (dregg-sdk/turn/circuit/verifier) are root-workspace members so THEIR
# `workspace = true` deps resolve against the root.
# FOLDED (formerly standalone-workspace / build-lock-escape crates, now members
# sharing the one target dir + one lock): chain, eth-lightclient, cosmos-lightclient,
# sel4/dregg-firmament, dregg-doc, crypto-hermine,
# constraint-lowering, fhegg-solver, git-deploy, deploy/gateway-ask, fhir,
# dregg-oracle, dregg-interchain-gov. The heavy ones (blst/alloy for eth,
# tendermint/ics23 for cosmos, alloy for chain, wgpu for fhegg-solver/fhir,
# tlsn-live for dregg-oracle, and the interchain-gov join) are
# members-but-NOT-default-members, exactly like the gpui/mozjs/servo elephants —
# (`forge-ci-runner` stays EXCLUDED — a fold was attempted + reverted: its
# src/materialize.rs uses a stale dregg-doc `AtomContent` API, a pre-existing
# source drift its isolation hid; fold it once that call is updated.)
# so the bare dev loop stays light. crypto-hermine (dregg-federation path-deps it)
# and fhegg-solver (circuit-prove dev-deps it) are now normal intra-workspace path
# deps; no more "multiple workspace roots". `fhegg-fhe` is now ALSO folded
# (members, NOT default-members — the heavy tfhe-rs FHE dep stays out of the
# bare dev loop): its `tfhe 1.6` needs `rayon ^1.11`, which was blocked only by
# poly-commitment's over-strict `rayon = "=1.10.0"` exact-pin. That pin is
# relaxed via the emberian/proof-systems fork ([patch] above); the root is now
# on rayon 1.12, so poly-commitment (storage kzg) and tfhe co-resolve.
# `orb` is EXCLUDED and is its own workspace root (`orb/Cargo.toml`, which carries
# the full reasoning). Short version: `orb/crates/dataplane/build.rs` hard-asserts
# `orb/.lake/build/lib/libdrorb.a` — a leanc archive cut by orb's own Lean project —
# so folding it in would make `cargo build --workspace` an unconditional red on any
# box that has not built orb's Lean; orb also pins its own toolchain
# (`nightly-2026-03-24` vs this tree's `nightly-2026-06-21`) and runs its own gate
# (`orb/scripts/ci.sh`). It is a cut of drorb that lives here, not a dregg crate.
# ⚠ It spent this whole window in NEITHER array: the ten `orb/crates/*` packages sit
# under this workspace's directory and declare no `[workspace]` of their own, so
# cargo claimed them and then could not find them — `cargo metadata` from inside any
# of them exited 101, `cargo build --workspace` compiled none of them, and 40k lines
# of a TLS/HTTP dataplane went un-type-checked and un-linted for the whole window.
# `scripts/check-workspace-closure.py` now fails on that third state, anywhere in
# the tree, so this cannot silently recur.
# `vendor/plonky3-fri-82cfad73` is a `[patch]` SOURCE that sat in neither array —
# the same third state as `orb`, found by the same gate. A patch target is not a
# member (nothing depends on it by path), so cargo claimed it from this root, could
# not find it listed, and refused: `cargo metadata` inside it exited 101.
# `vendor/bulletproofs-r1cs-wgpu` and `vendor/curve25519-dalek-dregg` were already
# excluded for exactly this reason; this one was missed.
#
# ⚠ `exclude` CANNOT reach a package nested inside a MEMBER's directory, and adding
# one there would be an inert line that reads like a decision. Cargo's rule
# (`WorkspaceRootConfig::is_excluded`) is `!explicit_member && excluded`, where
# `explicit_member` is a path-PREFIX test against every `members` entry — so
# `servo-render/vendor/servo-net` is "an explicit member" by virtue of living under
# `servo-render/`, and the exclude entry is silently discarded. The only mechanism
# that works there is an empty `[workspace]` table in the nested manifest itself,
# which is what `servo-render/vendor/servo-{net,paint}` and
# `starbridge-v2/vendor/pathfinder_simd` now carry.
exclude =["solana-lock", "solana-settlement", "wasm", "sdk-py", "pg-dregg", "poa-curator", "poa-solana-gate", "deos-zed-full", "discord-bot", "dregg-tui", "deos-homeserver", "durable-workflow", "forge-ci-runner", "orb", "vendor/bulletproofs-r1cs-wgpu", "vendor/curve25519-dalek-dregg", "vendor/plonky3-fri-82cfad73", "vendor/plonky3-challenger-82cfad73", "vendor/fhe-dregg"]

[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.94.0"
authors = ["Dregg Contributors"]
license = "AGPL-3.0-or-later"
homepage = "https://dregg.fg-goose.online"
repository = "https://github.com/emberian/dregg"

# ─────────────────────────────────────────────────────────────────────────────
# RELEASE MANAGEMENT — `dist` (formerly cargo-dist), config pinned to 0.31.0.
#
# This block governs the SHIPPABLE BINARIES of the dregg workspace. dist ships a
# package's binaries only when that package opts in; we set the workspace DEFAULT
# to `dist = false` and opt the two shippable crates IN at the package level
# (`cli/Cargo.toml` → bin `dregg`, `node/Cargo.toml` → bin `dregg-node`). Every
# other workspace member (demos, test harnesses, the discord bot, …) stays
# un-shipped. There is NO workspace-level package allowlist in dist 0.31 — the
# per-package `[package.metadata.dist] dist = true` is the only selector, and
# `repository` must resolve on each shipped package (cli adds
# `repository.workspace = true` for this; node already inherits it).
#
# `starbridge-v2` (the gpui native master interface) is now a workspace MEMBER
# (absorbed into the root workspace for the shared target dir), but dist still
# does NOT ship it: the workspace default is `dist = false` and starbridge-v2
# does NOT opt in (`[package.metadata.dist] dist = true` is the only selector, and
# it carries none), so dist reaches it via metadata yet emits no installer for it.
# It is gpui/Metal host-sensitive; its installers are packed by the bespoke
# `.github/workflows/starbridge-v2-installers.yml` (a separate lane), not by dist.
#
# TARGETS: the workspace target list is the CLI's broad set (macOS + Linux,
# x86_64 + aarch64, plus Windows-msvc). `dregg-node` LINKS THE VERIFIED LEAN
# ARCHIVE (libdregg_lean.a, a host-native static lib produced by the Lean
# compiler) and so CANNOT cross-compile; it overrides `targets` at the package
# level down to the two x86_64 host-native targets where a native CI runner +
# the Lean toolchain are available (aarch64 / Windows node builds are blocked on
# the Lean link — see node/Cargo.toml's per-package note).
#
# TOOLCHAIN: no `rust-toolchain-version` here (it is deprecated in 0.31). The
# generated `release.yml` carries no explicit `rustup default`, so CI honours the
# repo's root `rust-toolchain.toml` (nightly-2026-01-01) automatically. NOTE: the
# generated workflow installs Rust only; the `dregg-node` job additionally needs
# the Lean toolchain (elan/lake + warm mathlib) provisioned on the runner for its
# build.rs to splice libdregg_lean.a — a follow-up the bespoke starbridge
# workflow already models. The CLI job needs nothing beyond Rust.
#
# VERSION SCHEME: a dev pre-release tag is `v0.0.0-dev` (or `v0.0.0-dev.1`);
# `~` (as in the informal "v0.0.0~dev") is ILLEGAL SemVer and cargo/dist reject
# it. A `-dev`-suffixed tag is auto-marked a GitHub *prerelease* by dist. No crate
# version is bumped here — this only establishes the scheme; the crates remain
# at 0.1.0 until ember cuts a tag.
[workspace.metadata.dist]
# The preferred dist version to use in CI (`dist init` will fail to mismatch).
cargo-dist-version = "0.31.0"
# CI backend.
ci = "github"
# Installers to generate. shell + homebrew cover macOS/Linux cleanly. Windows is
# NOT given a native installer: `msi` requires per-package WiX GUIDs
# (upgrade-guid/path-guid) + bundle config that isn't clean yet, and `powershell`
# only activates with a Windows-installer story — so both are omitted honestly.
# The Windows CLI TARGET still builds (it produces a `.zip` artifact users can
# fetch directly); only the native Windows *installer* is deferred. Add `msi`
# back (with `dist init` to mint the GUIDs) when a clean Windows installer is
# wanted.
installers = ["shell", "homebrew"]
# Workspace target list = the CLI's broad set. Per-package `targets` narrows node.
targets = [
    "x86_64-apple-darwin",
    "aarch64-apple-darwin",
    "x86_64-unknown-linux-gnu",
    "aarch64-unknown-linux-gnu",
    "x86_64-pc-windows-msvc",
]
# Default: do NOT ship a package's binaries unless it opts in per-package.
dist = false
# Publish prereleases (e.g. a `v0.0.0-dev` tag) as GitHub prereleases.
pr-run-mode = "plan"

# ─────────────────────────────────────────────────────────────────────────────
# [patch] — UNIONED across the workspace (the 12 absorbed deos/starbridge crates
# each carried their own standalone-workspace [patch] tables before absorption;
# every relative `path` here is rewritten relative to THIS repo root).
#
# There are NO patch conflicts: every crate patched a given crate to the SAME
# source — the only differences were relative-depth artifacts pointing at one
# canonical location.
#
# The plonky3-recursion crates resolve PURELY from the git rev pinned in
# workspace.dependencies below (the pushed emberian/plonky3-recursion tip) — NO
# [patch]-to-sibling-path override, so a fresh clone and CI resolve identically.
# (The 2026-07-02 TEMP determinism-fix path-patch was REMOVED 2026-07-15: its fix,
# fork commit 993efec "deterministic verifier op-list", is an ANCESTOR of the pinned
# rev, so the git rev carries it. Tooth: circuit-prove/tests/recursion_vk_determinism.rs.)
# ─────────────────────────────────────────────────────────────────────────────
# o1-labs/proof-systems fork (emberian/proof-systems@relax-rayon-pin, rev
# c5305e63) — the ONLY delta from upstream rev 36a8b510 is one line: the
# workspace's `rayon = "=1.10.0"` EXACT pin relaxed to `rayon = "1"`. That
# exact-pin (rayon 1.10 -> 1.11 is API-stable; the pin was over-caution) made
# poly-commitment unco-installable with tfhe 1.6, which needs `rayon ^1.11` —
# the sole reason `fhegg-fhe` had to stay a standalone workspace. With the pin
# relaxed the root can carry rayon 1.11+ and poly-commitment (storage `kzg`) +
# tfhe (fhegg-fhe) resolve together. Patches the three used crates; content
# is byte-identical to 36a8b510 otherwise.
[patch."https://github.com/o1-labs/proof-systems"]
poly-commitment = { git = "https://github.com/emberian/proof-systems", rev = "c5305e63df5474c8cca81f7951264ea6aafe81db" }
mina-poseidon = { git = "https://github.com/emberian/proof-systems", rev = "c5305e63df5474c8cca81f7951264ea6aafe81db" }
mina-curves = { git = "https://github.com/emberian/proof-systems", rev = "c5305e63df5474c8cca81f7951264ea6aafe81db" }
# ─────────────────────────────────────────────────────────────────────────────
[patch."https://github.com/Plonky3/Plonky3"]
# Exact 82cfad73 `p3-fri` source with one additive backend-injection seam.  All
# other p3 crates remain pinned to that same git revision; the local crate's
# manifest repeats those pins so the field/MMCS types cannot split by source.
p3-fri = { path = "vendor/plonky3-fri-82cfad73" }
# ⚑ THE PROVER IS BYTE-DETERMINISTIC AGAIN. Exact 82cfad73 `p3-challenger` source with ONE
# delta: the five PoW `grind` sites take rayon's ORDERED finders (`find_first` /
# `find_map_first`) instead of the unordered `find_any` / `find_map_any`.
#
# Upstream returns whichever worker chunk finishes first, so two runs of the same binary on the
# same transcript pick different (equally valid) PoW witnesses. The witness is absorbed into
# Fiat-Shamir BEFORE the query indices are drawn, so the whole proof — opened values, varint
# lengths, total bytes — moves run to run. Measured 2026-07-31: three rayon runs, three distinct
# apex/terminal/shrink lengths; the serial fallback (which implements `find_any` as
# `Iterator::find`) reproduced its own bytes exactly. Two live byte-parity tests assumed
# determinism and flaked (`gpu_outer_config_synthetic_stark_byte_identical_to_cpu` 3/8;
# `decider_experiments::d3` 7/10), plus several `#[ignore]`d GPU parity gates.
#
# NOT a flag day and NOT a soundness change: no PoW knob moves, the verifier still checks only
# `check_witness` VALIDITY, and `recursion_vk_fingerprint` hashes circuit SHAPE (it excludes the
# pow witness, opened values and query proofs) so the VK was already invariant — verified
# identical across serial and both rayon runs in that measurement. What changes is that the
# emitted PROOF BYTES are now reproducible, and that rayon-on now agrees with rayon-off.
p3-challenger = { path = "vendor/plonky3-challenger-82cfad73" }
# ─────────────────────────────────────────────────────────────────────────────
[patch.crates-io]
# Exact fhe 0.1.1 source with ONE additive, feature-gated seam: a canonical wire
# codec for `mbfv::RelinKeyShare` (`mbfv-share-codec`). Upstream makes the
# multiparty relinearization share fully opaque — `pub(crate)` fields, no
# accessor, no Serialize impl anywhere in `mbfv` — so the relin ceremony
# physically cannot cross a process boundary, and the dregg committee runs its
# parties as SEPARATE PROCESSES. The seam adds no cryptography: polynomials ride
# `fhe_math::rq::Poly`'s own public exact round-trip codec and the rebuilt share
# is the struct the in-process path already builds. All BFV/mbfv protocol code is
# byte-identical to 0.1.1. See vendor/fhe-dregg/Cargo.toml for the full delta.
fhe = { path = "vendor/fhe-dregg" }
# Exact curve25519-dalek 4.1.3 source with one additive, feature-gated
# qualification API for canonical extended-coordinate GPU parity. All normal
# dalek arithmetic and encodings remain upstream-identical.
curve25519-dalek = { path = "vendor/curve25519-dalek-dregg" }
# ark-serialize fork — the `serde_with` feature `dregg-hints` needs (emberian/algebra).
ark-serialize = { git = "https://github.com/emberian/algebra", branch = "serde-integration" }
ark-serialize-derive = { git = "https://github.com/emberian/algebra", branch = "serde-integration" }
# The two zed-forked async-* crates the gpui dep tree's `[patch.crates-io]` pins
# (gpui's vendored `util` calls `smol::process::Child::adopt_raw_pid`, present only
# on these forks). Inert when the gpui-bearing crates are not in the build graph.
async-process = { git = "https://github.com/zed-industries/async-process.git", rev = "0b6d6713570af61806e1e5cb40e0f757cb93fd9d" }
async-task = { git = "https://github.com/smol-rs/async-task.git", rev = "b4486cd71e4e94fbda54ce6302444de14f4d190e" }
# Vendored pathfinder_simd 0.5.6 (gpui → font-kit → pathfinder): scalar-path build
# (the nightly portable-SIMD intrinsics were removed from current toolchains). The
# one canonical vendored copy lives under starbridge-v2.
pathfinder_simd = { path = "starbridge-v2/vendor/pathfinder_simd" }
# stylo `ToCss`-derive fork (emberian/stylo@ember-nightly-fix) — pins the `?`
# residual error type to `core::fmt::Error`, defeating the serde_fmt ambiguity in
# the full cockpit graph (web-shell → servo → stylo). Inert outside that graph.
stylo_derive = { git = "https://github.com/emberian/stylo", rev = "abc53ac61" }
# servo-paint SWGL fork (servo-render's libservo path): WebRenderOptions
# `clear_caches_with_quads: false` so SWGL's DepthFunc assert does not SIGABRT.
servo-paint = { path = "servo-render/vendor/servo-paint" }
# servo-net forbidden-schemes fork (servo-render's libservo path): allow an
# embedder ProtocolHandler for http/https (the cap-gated byte socket).
servo-net = { path = "servo-render/vendor/servo-net" }

# The gpui fork (emberian/zed) resolves directly from git at the rev pinned in
# starbridge-v2/Cargo.toml (the offscreen/headless wgpu renderer the render-capture
# bakes need is on that pushed rev) — no local-path [patch] override.


[workspace.dependencies]
## Local
dregg-macaroon = { path = "macaroon", default-features = false }
dregg-types = { path = "types" }

## Plonky3 core (rev 82cfad73 — the version dregg proofs were validated against)
p3-air = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-baby-bear = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-batch-stark = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-bn254 = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-challenger = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-commit = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-dft = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-field = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-fri = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-lookup = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
# ⚑ REAL PARALLELISM. Measured 2026-07-31: p3-maybe-rayon resolved with `default` only,
# so every par_* in p3-dft/merkle/fri/uni-stark was the SERIAL fallback (finding was
# quoted and left undone). Enabling `parallel` here forces rayon on across the whole
# resolve by feature unification. Deterministic (par over independent chunks) — the
# bit-exact CPU-parity discipline is the guard; the proof/VK must stay byte-identical.
p3-maybe-rayon = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec", features = ["parallel"] }
p3-matrix = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-mds = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-merkle-tree = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-monty-31 = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
# `Poly` / `Point` — the multilinear evaluation-table types the sumcheck engine
# consumes. Already resolved at this rev as a transitive dep of `p3-commit`;
# named here so `sumcheck-toy` can depend on it directly and cannot drift to a
# second source.
p3-multilinear-util = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-poseidon2 = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
# The sumcheck engine (prover + verifier + the WHIR-shaped `layout` machinery).
# It EXISTS at the pinned Plonky3 rev — it is NOT in the emberian/plonky3-recursion
# fork, so this row must point at Plonky3/Plonky3 like the rest of the core block.
# `parallel` matches the p3-maybe-rayon row above; feature unification would turn
# it on regardless, so this is a statement of intent, not a behaviour change.
p3-sumcheck = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec", features = ["parallel"] }
p3-symmetric = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-uni-stark = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }
p3-util = { git = "https://github.com/Plonky3/Plonky3", rev = "82cfad73cd734d37a0d51953094f970c531817ec" }

## Plonky3-recursion fork — rev fc3c6df (`update-plonky3-rev` tip). PURE git deps, no
## `[patch]`, no sibling checkout — a fresh clone and CI resolve this identically.
##
## fc3c6df: a constant's VALUE is now a PREPROCESSED column of `ConstAir`
## (`[ext_mult, out_idx, value[0..D]]`, was `[ext_mult, out_idx]`) with D degree-1
## constraints `main.value == prep.value`. Before it, the value lived only in the
## constraint-free main trace, so two circuits differing ONLY in a constant had identical
## preprocessed commitments — identical VK cap, identical vk-spine, identical
## whole-chain anchor. ⚑ FLAG DAY: every preprocessed commitment moves, so every
## `RecursionVk` and every anchor derived from one changes. Joins the spine's staged
## rotation (`e1d8ab9bc`) rather than adding a second one.
## 4aead01: child VK cap targets surfaced to the expose hooks (the spine's substrate).
## 0a4a554: GPU recursion prove path; `RecursionOutput` carries
## `Option<Rc<CircuitProverData>>`. Descends from 993efec's deterministic verifier op-list.
p3-recursion = { git = "https://github.com/emberian/plonky3-recursion", rev = "fc3c6df" }
p3-circuit = { git = "https://github.com/emberian/plonky3-recursion", rev = "fc3c6df" }
p3-circuit-prover = { git = "https://github.com/emberian/plonky3-recursion", rev = "fc3c6df" }
p3-poseidon2-circuit-air = { git = "https://github.com/emberian/plonky3-recursion", rev = "fc3c6df" }

## Crates.io
## --- TEMP LOCAL DSU-FIX ITERATION PATCH (REMOVE before CI-correct rev bump) ---
ark-serialize = { version = "0.5", features = ["derive"] }
# NOTE: RC needed for aead 0.6 trait unification with chacha20poly1305 0.10;
# stable aes-gcm 0.10 uses aead 0.5 which conflicts. Revisit when 0.11 stable ships.
aes-gcm = { version = "0.11.0-rc.3" }
base64 = { version = "0.22" }
chacha20poly1305 = { version = "0.10" }
criterion = { version = "0.5", features = ["html_reports"] }
proptest = { version = "1" }
directories = { version = "6" }
getrandom = { version = "0.3" }
globset = { version = "0.4" }
hmac = { version = "0.12" }
keyring = { version = "3", features = ["sync-secret-service", "crypto-rust"] }
rmp-serde = { version = "1.3.1" }
scalable_cuckoo_filter = { version = "0.5", features = ["serde_support"] }
serde = { version = "1", default-features = false, features = ["derive"] }
serde_json = { version = "1" }
sha2 = { version = "0.10" }
subtle = { version = "2" }
tempfile = { version = "3" }
thiserror = { version = "2" }
tracing = { version = "0.1" }
x25519-dalek = { version = "2", features = ["static_secrets"] }
zeroize = { version = "1", features = ["std", "derive"] }

[workspace.lints.clippy]
all = { level = "warn", priority = -2 }
# `deny` (escalated from the `all = warn` blanket, 2026-07-25).
#
# `clippy::correctness` is DENY-BY-DEFAULT upstream. Setting `all = "warn"` above
# silently DOWNGRADED it, and the whole group then drowned in ~4000 lines of style
# warnings that nobody reads. What that cost, concretely: `dreggnet-party`'s Scout
# `UnsafeStrike` preflight was `hp > FINISH_THRESHOLD && hp <= ATTACK_DIE` with both
# constants equal to `8` — an UNSATISFIABLE guard, i.e. a check that could never
# refuse anything. `impossible_comparisons` had been naming it, at warn level, in
# every build, for as long as it existed.
#
# This group is exactly the "a guard that cannot fire" detector: `impossible_comparisons`,
# `absurd_extreme_comparisons`, `never_loop`, `unit_cmp`, `eq_op`, `ifs_same_cond`,
# `bad_bit_mask`, `reversed_empty_ranges`, `while_immutable_condition`, `min_max`,
# `erasing_op`. Every one of them describes code that LOOKS like a check and is not.
# Same discipline (and same reasoning) as `unexpected_cfgs = "deny"` below.
#
# Priorities: cargo requires every lint GROUP to sit strictly below every individual
# lint, so the per-lint `allow`s stay at 0, `correctness` at -1, and `all` at -2.
# Emission order is ascending priority, so `all = warn` lands first and `correctness
# = deny` overrides it for that subgroup only; the style/pedantic half of `all`
# stays advisory, and an explicit per-lint `allow` still beats both. If a correctness
# lint is genuinely wanted, `#[expect(clippy::…, reason = "…")]` at the site —
# `expect`, not `allow`, so it self-retires when the shape changes. Verified over
# `cargo clippy --workspace --all-targets --keep-going` on 2026-07-25.
correctness = { level = "deny", priority = -1 }

# `deny` (escalated from the `all = warn` blanket, 2026-08-08). NOT a correctness lint — it is
# `style` — so it needs its own row.
#
# ⚑ `assertions_on_constants` prints TWO messages and both are this lint: "this assertion has a
# constant value" (a named-constant condition) and "this assertion is always `true`/`false`" (a
# literal one). An `assert!` whose condition is decided entirely by constants is not a test. It
# reports — after the whole crate has already compiled against the fact, and only if somebody runs
# that test — something the BUILD could have refused outright.
#
# What the 2026-08-08 sweep found in 227 members: 45 + 3 sites, and they were not decoration.
#   · `circuit/src/effect_vm/verify.rs` — the balance-limb PI gate's reachability precondition,
#     which had ALREADY SHIPPED broken (demanding 201 felts of a 35-felt leg, so the range check
#     answered a length complaint about every proof it was meant to police).
#   · two range-gadget non-vacuity guards (shielded attest, solvency) wearing
#     `#[allow(clippy::assertions_on_constants)]` — the `allow` WAS the wrong answer to the lint.
#   · `deos-js/src/mud.rs` — a `debug_assert!` over a slot-count premise, compiled out of every
#     release binary.
#   · `node/src/poa_strand_admission.rs` — `assert!(POA_BOND_ADMISSION_DISABLED)` restating
#     `pub const POA_BOND_ADMISSION_DISABLED: bool = true`, in a test whose name promises "no bond
#     surface exists".
#   · `game-turn-slice` — `assert!(3i128 - 5i128 < 0)` standing in for a forged range head.
#
# ⚠ The lint's OWN suggestion is the fix ("consider moving this to an anonymous constant:
# `const _: () = { assert!(..); }`"), so an `expect` at a site is almost never right here: if the
# condition really is constant, the obligation really does belong to the build, and `const _`
# is strictly stronger than the `#[test]` it replaces. Cost measured at escalation over
# `cargo clippy --workspace --all-targets --keep-going`: ZERO remaining sites, ZERO `expect`s.
assertions_on_constants = "deny"
assign_op_pattern = "allow"
# Stylistic `chunks_exact(N)` -> `as_chunks::<N>()` nudge; a fresh clippy flags it
# but it's a readability preference, not a correctness lint — allowed like the rest.
chunks_exact_to_as_chunks = "allow"
collapsible_if = "allow"
collapsible_match = "allow"
derivable_impls = "allow"
explicit_auto_deref = "allow"
for_kv_map = "allow"
get_first = "allow"
if_same_then_else = "allow"
int_plus_one = "allow"
iter_overeager_cloned = "allow"
manual_is_multiple_of = "allow"
mem_replace_with_default = "allow"
needless_borrow = "allow"
needless_borrows_for_generic_args = "allow"
needless_range_loop = "allow"
new_without_default = "allow"
ptr_arg = "allow"
question_mark = "allow"
redundant_closure = "allow"
should_implement_trait = "allow"
single_match = "allow"
too_many_arguments = "allow"
type_complexity = "allow"
unnecessary_cast = "allow"
unnecessary_map_or = "allow"
unwrap_or_default = "allow"
useless_conversion = "allow"
useless_format = "allow"
vec_init_then_push = "allow"
wrong_self_convention = "allow"
identity_op = "allow"
manual_strip = "allow"
result_large_err = "allow"
doc_nested_refdefs = "allow"
large_enum_variant = "allow"
clone_on_copy = "allow"
extend_with_drain = "allow"
redundant_pattern_matching = "allow"
while_let_loop = "allow"
io_other_error = "allow"
manual_unwrap_or_default = "allow"
manual_unwrap_or = "allow"
manual_flatten = "allow"
print_literal = "allow"
manual_saturating_arithmetic = "allow"
large_const_arrays = "allow"
unused_enumerate_index = "allow"

[workspace.lints.rust]
# `deny` (escalated from "warn" 2026-07-17; flipped from "allow" 2026-07-16, ember-approved).
# `unexpected_cfgs` fires on a `cfg(feature = "X")` where X is NOT a declared feature — always-false, so
# the gated code silently compiles to NOTHING. That is how a fail-open peer-exchange guard (cell-crypto,
# `zkvm` — no such feature), the "production PIR discovery transport" (dregg-sdk-net, `reqwest` — a
# non-optional dep, not a feature), and the never-compiled `gen_plonky3` DSL backend (emitted code that
# didn't build; deleted 43b930241) all vanished from every build undetected. The surfaced set is cleared,
# so the class is now a build error. NOTE: this binds only crates with `[lints] workspace = true` —
# every member has it (added 2026-07-17) except crates whose manifests other lanes held dirty at the
# time (see TESTQALOG 2026-07-17 board/lane-a); vendored third-party (starbridge-v2/vendor/*) keeps
# rustc's default warn with its real build-script cfgs declared in its own manifest.
unexpected_cfgs = "deny"

[profile.bench]
opt-level = 3
lto = "thin"
codegen-units = 1

# Incremental compilation is OFF for dev/test. The default-on incremental
# partitioner interacts badly with the opt-level asymmetry below: the
# `dregg-circuit` lib is opt-level 3 (rustc disables `share-generics` export at
# opt>=2, so the rlib does NOT emit shared generic monomorphizations), while the
# integration-test binaries are opt-level 0 (`share-generics` import ON, so they
# emit only *references* to those upstream instantiations). Under incremental,
# rustc's stale-CGU reuse drops the object that would define a referenced p3
# generic (e.g. `BinomialExtensionField::from_ext_basis_coefficients`,
# `p3_uni_stark::verifier::recompose_quotient_from_chunks`,
# `p3_batch_stark::symbolic::get_max_constraint_degree`) while keeping the
# reference in a sibling `.rcgu.o` — yielding undefined-symbol link failures on
# the prover-feature test umbrella (`anon.*`/`.llvm.<hash>` dangling locals).
# Disabling incremental forces a clean per-build partition so every monomorph is
# emitted. Cheap here: all packages + deps already compile at opt-level 3 below,
# so incremental's reuse buys little, and a green link is the floor everything
# rides on. (Witness: `CARGO_INCREMENTAL=0 cargo build … --tests` links clean.)
[profile.dev]
incremental = false

# DEBUG INFO: line tables only. Nothing in this repo reads full DWARF, and full
# DWARF was 83-92% of every debug binary.
#
# MEASURED — a controlled A/B on ONE artifact, one lane (persvati:darkpool), one
# revision (03759c3a2), identical features, this key the only variable. Both
# binaries were kept side by side; reproduce with `size -A <binary>`:
#
#                        debug=2 (was)   line-tables-only (is)    delta
#     file on disk        1570.6 MB          665.0 MB           2.36x smaller
#     DWARF total         1249.7 MiB         388.9 MiB          3.21x smaller
#     .debug_str           490.4 MiB         164.1 MiB           -66.5%
#     .debug_info          375.3 MiB          90.5 MiB           -75.9%
#     .debug_loc           225.6 MiB          17.8 MiB           -92.1%
#     .debug_ranges         78.7 MiB          45.7 MiB           -41.9%
#     .debug_line           70.5 MiB          64.3 MiB            -8.9%
#     .text                107.4 MiB         104.5 MiB            -2.8%
#     DWARF as % of file      83.4%             61.3%
#
# The 84% is not one unlucky binary: the 20 largest ELF artifacts in the lane
# measured 83.4-84.5% DWARF each, and all 528 linked binaries under target/debug
# totalled 168.6 GiB of which 152.8 GiB (90.6%) was DWARF.
#
# THE .text WOBBLE IS NOT DEAD-CODE RETENTION — it was worth checking, because if
# `-g` had been keeping otherwise-collectable code alive, dropping it would have
# been changing what we SHIP. It is not: over the 212,534 function names present in
# both binaries, 69.8% are byte-identical in size, 29.2% are smaller under
# line-tables-only, 0.9% larger, netting -2.76% — which is the entire section
# delta. The smaller binary has slightly MORE `.text` symbols (303,546 vs
# 303,045), so nothing was dropped; per-function codegen simply differs under
# `-g`, concentrated in heavily-inlined functions. Which LLVM pass is responsible
# is NOT isolated.
#
# STILL DWARF AFTER THIS: 61.3% of the remainder, dominated by `.debug_str`
# (164.1 MiB of mangled names for the subprogram/inlined-frame skeleton) and
# `.debug_line` (64.3 MiB). Those two ARE the backtrace-attribution machinery, so
# this key is the end of the free win, not the end of the weight. `debug = 0`
# would take the file to roughly 245 MiB by section arithmetic (UNBUILT — not a
# measurement), at the cost of file:line on backtrace frames. That is a real
# trade, so it is ember's call, not a default.
#
# `.debug_loc` is 226 MB because of the opt-level = 3 below, NOT because of this
# key: an optimized variable needs a location LIST per live range instead of one
# stack slot. So the two settings compound, and neither is the other's fault.
#
# WHAT THIS DOES NOT COST US. Verified by experiment, not assumed:
#   - PANIC LOCATIONS SURVIVE. `panicked at foo.rs:6:27` and the full `assert_eq!`
#     left/right dump are `#[track_caller]` + static `core::panic::Location` data
#     baked into .rodata. They are not DWARF; they are byte-identical even at
#     `debuginfo=0`. Every panic-based tooth in the tree keeps its teeth.
#   - BACKTRACE FUNCTION NAMES SURVIVE: they come from `.symtab`, also not DWARF.
#     (Which is why `strip` is NOT set here and must not be — that WOULD take
#     them. Nothing above deletes symbols.)
#   - `debug = "line-tables-only"` keeps `.debug_line`, so a backtrace FRAME can
#     still resolve to file:line. Full `debuginfo` buys variable inspection in a
#     debugger, and the tree has no debugger consumer: zero rust-gdb/rust-lldb
#     invocations, zero `Backtrace::capture`, zero debugger config files.
# If a debugging session ever needs locals, get them per-invocation without
# editing this file: `CARGO_PROFILE_DEV_DEBUG=2 cargo test …`.
#
# Package overrides inherit every key they do not name, so the `opt-level = 3`
# blocks below pick this up too — deps included. `profile.test` inherits `dev`.
# `release`/`bench` are untouched (they already default to no debug info).
#
# THIS DOES NOT REACH THE EXCLUDED WORKSPACES. All 11 of them carry their own
# `[workspace]` and none sets `debug`, so each still defaults to full DWARF:
# solana-lock, solana-settlement, wasm, sdk-py, pg-dregg, deos-zed-full,
# discord-bot, dregg-tui, deos-homeserver, durable-workflow, forge-ci-runner.
# A profile key cannot cross a workspace root; each needs its own line.
#
# NOT set: `split-debuginfo` — deliberately, and NOT because it is worthless.
# It is platform-divergent (macOS already defaults to `unpacked`, which is exactly
# why a Mac binary never carried this weight while a Linux one did; on Linux the
# default is `off`, so the linker copies every dep's DWARF into every binary).
# Measured on this lane: the 20 largest ELF artifacts are 28.8 GiB holding 24.2 GiB
# of DWARF (84.0%), and they are 20 near-identical copies of the SAME dependency
# DWARF — which is the shape split-DWARF deduplicates, since `.dwo` sidecars are
# emitted once per crate rather than copied once per linked binary. So it is a
# plausibly large second win, not a wash.
# It stays unset because it is UNVERIFIED at workspace scale: `off`/`packed`/
# `unpacked` were each confirmed to link and to keep panic locations on Linux
# x86_64 AND macOS arm64, but only on a standalone test crate, and the toy cannot
# size the effect (its DWARF comes from precompiled std, which split-DWARF does
# not touch). Note `.debug_line` CANNOT move to a `.dwo`, so this compounds with
# the key above rather than replacing it. Land it behind a real measurement.
debug = "line-tables-only"

# Fast dev/test builds with FAST crypto: keep our own crates cheap to compile
# (opt-level 0, quick iteration) but build all DEPENDENCIES optimized so the
# crypto primitives (plonky3 p3-*, poseidon2, BLS12-381, FRI) don't run
# unoptimized in debug — a debug-build STARK verify/prove pins a core for ages
# otherwise (e.g. dregg-lightclient tests). opt-level 3 deps + opt-level 0 ours
# = the best of both. Applies to the `test` profile too (it inherits `dev`).
[profile.dev.package."*"]
opt-level = 3

# The crypto-heavy WORKSPACE crates also get optimized in dev/test (they hold
# hot proving/verifying loops that the `*` glob above does not cover for
# workspace members). Compile cost is amortized by caching; runtime is night/day.
[profile.dev.package.dregg-circuit]
opt-level = 3
[profile.dev.package.dregg-lightclient]
opt-level = 3
# `dregg-circuit-prove` holds the recursion proving + AGGREGATION hot path —
# `aggregate_tree`/`merge_two_segment_proofs`, the in-circuit `seg_poseidon_commit`
# sponge + `segment_combine_expose`, the descriptor-leaf AIR construction. At
# opt-level 0 a single whole-chain fold pins a core for tens of minutes (the debug
# `-p dregg-lightclient` / `ivc_turn_chain` recursion tests never finish); at
# opt-level 3 it is the usual minutes. ALWAYS optimize it.
[profile.dev.package.dregg-circuit-prove]
opt-level = 3
